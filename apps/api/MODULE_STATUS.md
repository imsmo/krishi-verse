# Krishi-Verse — Module Build Status

Single source of truth for build progress. Update every session.
Legend: ⬜ not started (stub) · 🟡 in progress · ✅ done (tsc clean + unit + integration tests green, registered in app.module, README written).

A module is **✅ only when**: domain invariants + state machine, repo (tenant_id everywhere, optimistic lock), service (UoW tx + outbox-in-tx + idempotency + quota + enforced authz + metrics), controller (guards + zod + idempotency + versioned), typed errors, unit + tenant-isolation + integration(RLS) tests, slice SQL, README — all per the Build Playbook §4 Definition of Done.

## Core platform (Track A)
| Area | Status | Notes |
|---|---|---|
| database (UoW/RLS/pools/replica/shard-router) | ✅ | concrete Postgres impls |
| outbox writer | ✅ | Pg, writes in-tx |
| outbox dispatcher (relay + handler registry) | ✅ | transactional relay, FOR UPDATE SKIP LOCKED, idempotent handlers, DLQ-on-fail; kv_relay BYPASSRLS (0018); runRelay poller |
| quota / idempotency / cache / metrics | ✅ | Pg + Redis/in-memory + Prom |
| auth guards + tenant-context + RBAC guard | ✅ | JWT verify; perms DB-resolved at mint (RoleCache) |
| http (exception filter, response envelope, request-id, zod) | ✅ | |
| config / health | ✅ | |
| audit-log writer | ✅ | built with identity |
| rate-limit guard | ✅ | global edge limit + per-route (auth) |
| feature-flags | ✅ | DB-backed, kill-switch, rollout, @FeatureFlag guard |
| i18n | ✅ | en/hi/gu + exception-filter localization |
| media / S3 presign | ✅ | self-contained SigV4 presign (no SDK); scan-gated download (fail closed); MIME allow-list + size cap; RLS; `putGeneratedDocument` for PDFs |
| PDF renderer (statements/invoices) | ✅ | self-contained PDF writer (no lib); DocumentPdfService renders + stores via media (`document_pdfs` flag); `pdf_media_id` set |
| EXIF stripper + image-processing job | ✅ | JPEG/PNG metadata surgery (no codec); ImageProcessingJob strips clean images → `exif_stripped`. Thumbnail resize deferred (needs codec) |
| RBAC role-cache (DB-backed perms) | ✅ | built with identity |
| auth login (OTP, refresh token, jwt.strategy) | ✅ | built with identity |
| resilience (circuit-breaker/retry/bulkhead/timeout) | ✅ | composed `ResilienceService` (RESILIENCE token) + tests |
| wallet boundary (core/wallet) | ✅ | double-entry, zero-sum, hash-chained, idempotent; in-process client = wallet gRPC contract |
| reconciliation (zero-sum + internal-balance → reconciliation_runs) | ✅ | continuous money-safety net; mismatch detection proven |
| commission/tax engine (settlement split) | ✅ | effective-dated rule resolution + zero-sum split (seller/commission/GST/TDS); `commission_split` flag |
| billing documents (settlement statements + GST invoices) | ✅ | per-order settlement_lines → seller statement (next_doc_number); buyer GST invoice per order (CGST/SGST); ownership-gated; PDF/IRN/GSTIN deferred |
| buyer-side charge engine (delivery + platform fee) | ✅ | charge_definitions resolution (flat/percent/slab/per_unit); applied at checkout (`buyer_charges` flag); settlement routes charges to platform, excludes from seller gross (zero-sum) |
| backpressure (load-shedder/priority/concurrency) | ⬜ | before first big load |
| sharding execution (pool-mgr/directory/cross-shard) | ⬜ | Phase 3 trigger |
| OpenSearch index-builders | ✅ | core/search: resilience-wrapped transport + tenant-safe query client (mandatory tenant_id IN [caller,__platform__] filter, search_after keyset, size-capped) + IndexDef registry (listings/products: mappings/analyzers + projector + isIndexable + event types) + IndexBuilderService (ensureIndices + re-read-and-upsert-or-drop syncById) + outbox→index handlers (flag-gated `search_indexing`, idempotent) + bounded keyset reindex job. Config-driven: no OPENSEARCH_URL ⇒ NullSearchClient replica fallback. price_minor stays string (Law 2). Unit + real-cluster tenant-isolation integration test (gated on OPENSEARCH_URL) |
| bulk CSV import | ✅ | core/bulk: migration 0030 (bulk_import_jobs + bulk_import_errors, tenant-scoped + RLS) + job lifecycle state machine (pending→processing→completed|partially_completed|failed, +cancel) + self-contained bounded RFC-4180 CSV parser + per-row result store (capped 1000) + pluggable BulkApplierRegistry (modules register appliers; catalogue ships the `products` applier delegating to ProductService) + BulkImportProcessor (claim FOR UPDATE → fetch via ObjectStore → parse → per-row apply with deterministic idem key → finish) + v1 controller (create/list/get/errors/cancel) + worker sweep (FOR UPDATE SKIP LOCKED). `bulk_import` flag + bulk.import perm (tenant_admin); idempotent create; active-jobs cap; audit; keyset; cross-tenant RLS proven. Stale-processing recovery + resumable mid-file deferred |
| cells (per-country) | ⬜ | Phase 4 |

## Business modules (Track B)
| # | Module | Wave | Status |
|---|---|---|---|
| 1 | identity | 1 | ✅ |
| 2 | catalogue | 1 | ✅ |
| 3 | **listings** | 1 | ✅ |
| 4 | orders | 1 | ✅ |
| 5 | payments (+ wallet-service) | 1 | ✅ (money-in: intents/webhook/refund; money-out: payouts + RazorpayX execution + failure-reversal; escrow settlement via outbox relay + commission/tax split; reconciliation; statements + GST trade-invoices + buyer-charges. **Invoicing ✅ API-W3-07**: trade-invoice/settlement-statement/charge-definition/tax-rule domain value objects (bigint, zero-sum/GST-split invariants) + tenant commission-rule catalog (overrides only; platform defaults god-mode/admin-api, mutate→404) + settlement-statements worker run. **Payout-batch ✅ API-W3-08**: PayoutBatch aggregate (open→executing→executed/failed) + batch run (claim queued FOR UPDATE SKIP LOCKED → disburse via wallet boundary → record total/count); wage priority lane (booking-clocked-out handler promotes `labour.wages_paid` payouts behind `wage_priority_payout`, in-module only, Law 11) + wage-lane job; async RazorpayX payout webhook (processed→ledger move / failed→reverse, HMAC + signed-notes tenant, idempotent on event id + wallet keys, ingress `…/webhooks/:provider/payouts`); payout-queue-monitor + daily-gateway-recon jobs; PaymentsPublisher + WalletBalanceReadModel. **Money-insights + autopay ✅ P0-8**: `WalletInsightsReadModel` (caller's-OWN earnings credits / spending debits, aggregated FLOAT-FREE on the replica via `SUM(...)::text`, anti-IDOR join on `wallet_accounts.owner_user_id`, bounded `resolveWindow` default ~12mo/clamp ≤3y) → `GET v1/wallet/earnings|spending-insights`; UPI-autopay mandates (migration 0049, tenant+RLS, tokenised `provider_mandate_ref` + MASKED `vpa_masked` (raw VPA never stored/logged), bigint `max_amount_minor`, `mandate.state` pending→active→paused→cancelled/expired, optimistic `version`, one-live-per-purpose) — `Mandate` aggregate + `MandateService` (UoW+outbox+idempotency+audit) + `AutopayController` `POST/GET/DELETE v1/wallet/autopay` (online_payments flag). **FLAGGED:** auto-debit *collection* needs a UPI-AutoPay PSP + webhook + worker (registering records the standing instruction only). gRPC-extraction/striping/e-invoice-IRN = next wave) |
| 6 | auctions | 2 | ✅ (english/sealed; EMD holds + anti-snipe + reserve/approval; `auctions` flag. reverse/dutch + order-from-win + watchers = next wave) |
| 7 | offers | 2 | ✅ (buyer↔seller price negotiation: make/counter/accept/reject + turn model + 72h expiry job; `offers` flag; no money. accept→order via outbox relay: orders OfferAcceptedHandler creates the order (source=offer, offer_id), offers OrderFromOfferCreatedHandler links converted_order_id) |
| 8 | requirements | 2 | ✅ (reverse marketplace: buyer posts demand, sellers quote, buyer shortlists/accepts → requirement fulfilled; `requirements` flag; no money. accept→order via outbox relay: orders QuoteAcceptedHandler creates the order (source=requirement, requirement_id). expiry job for need_by/valid_until. match-notifications deferred) |
| 9 | logistics | 2 | ✅ (shipment fulfilment spine: order_confirmed→auto-shipment, assign/pickup/dispatch, OTP-gated proof-of-delivery→order delivered via outbox relay; `logistics` flag + `logistics.manage` perm + rider authz. **Fleet registry ✅ API-W3-03**: partners (3pl/tenant_fleet/rider, hybrid-tenant Law 11) + vehicles (reg-no unique-per-partner→409) + seller pickup-slots. **Zones-routing ✅ API-W3-04**: delivery zones (pincode serviceability/charge zoning) + Saturday Village Run routes + append-only cold-chain telemetry (partitioned; breach computed at record, worker alerts once via ops_job_runs watermark; village-run job idempotent per date) — full CRUD + outbox + audit + RLS/breach-dedup/authz tests. Only OTP-SMS dispatch + COD recon remain (land with notifications/payments)) |
| 10 | reviews | 2 | ✅ (two-sided verified-purchase ratings: order_completed→eligibility (0024), buyer↔seller reviews w/ server-resolved target, edit/seller-response/moderation, cached avg/count/histogram; `reviews` flag + review.create/review.moderate perms. helpful-votes/booking-reviews/AI-premod deferred) |
| 11 | disputes | 2 | ✅ (order disputes: delivered→eligibility (0025), party raises vs counterparty, threaded evidence, moderator review/escalate/resolve; opening pauses order (→disputed), resolve drives order refunded/released via outbox; `disputes` flag + dispute.raise/dispute.resolve perms. wallet-reversal LIVE (escrow→buyer, `dispute_refunds` flag) incl. refund_partial remainder re-settlement + precise post-settlement clawback (0026 reversible lines); returns flow deferred) |
| 12 | promotions | 2 | ✅ (coupon discount engine: budgeted promotions + coupon codes (global/per-user caps), validate preview + atomic redeem (FOR UPDATE, append-only redemption, budget/caps fail-closed); `promotions` flag + promotion.manage perm; no wallet (discount = price reduction). checkout wiring DONE (CheckoutService applies couponCode → order.discount_minor in-tx); cashback wallet-credit deferred) |
| 13 | memberships | 2 | ✅ (subscription tiers (free/wallet-paid) + benefits; admin tier CRUD (membership.manage), self-serve subscribe (wallet debit userMain→fees, zero-sum) / renew / cancel, one-live-per-user, expiry job; `memberships` flag. member-fee-override at checkout DONE (freeDelivery + platform_fee_bps_override, `memberships` flag); card-payment activation + auto-renew deferred) |
| 14 | labour | 3 | ✅ (dignified-work spine: worker profiles + bookings + assignments + wage settlement. THE DIGNITY FLOOR enforced 3× (service snapshot from minimum_wages → domain 422 → chk_dignity_floor in DB). Wage payout LIVE: employer userMain → worker userMain, txnType wage_payout, zero-sum + idempotent (Law 2). age_verified_18 HARD assign gate; worker consent; optimistic-lock bookings; respond-timeout job; `labour` flag + worker.book/booking.manage perms. **Attendance lifecycle ✅ P0-9**: geofenced clock-in → clock-out (SERVER-stamped time + FLOAT-FREE hours/overtime via pure `hours.ts`, 8h standard split, break-aware) → EMPLOYER dual-confirm (booking owner OR booking.manage admin, Law 11; must be clocked-out first; in-tx audit row) → worker work-history read-model (keyset, anti-IDOR); derived state machine `clocked_in→clocked_out→confirmed` (terminal/immutable); routes `POST :id/attendance/clock-out|confirm` + `GET attendance/history`; idempotent + guarded (no double clock-out, no confirm-before-clock-out, foreign-worker 403, non-employer 404). Wage settlement unchanged (ledger-only; confirm is the gate it reads). advances/insurance/MGNREGA/crews/sardars/grievances/migrant/skills+min-wage admin CRUD deferred) |
| 15 | livestock | 3 | ✅ (animal registry + VET MARKETPLACE: species/breeds master data, farmer animals (Pashu Aadhaar, active→sold/deceased/lost), vet profiles + priced service catalog, vet bookings (requested→accepted→en_route→in_consult→prescribed→completed). Vet-fee settlement LIVE: farmer userMain → vet userMain, txnType service_fee, zero-sum + idempotent (Law 2), fee snapshotted from service price; `livestock` flag + animal.manage/vet.book/vet.manage perms. health events(partitioned)/ownership transfers/prescriptions/semen+AI/outbreaks/reminder+INAPH jobs/vet-fee commission deferred; all DAIRY tables → M16) |
| 16 | dairy | 3 | ✅ (milk procurement spine: MCC centres + memberships + quality rate cards (FLOAT-FREE bigint pricing engine: two_axis/fat_pooled/snf_pooled + base) + partitioned milk collections (priced at counter, idempotent per member/shift/day) + per-cycle milk bills (draft→previewed→approved→paid). Payout LIVE: tenant main → farmer userMain, txnType milk_payment, zero-sum + idempotent (Law 2), net = gross − deductions; cycle-close job; `dairy` flag + dairy.manage perm. BMC cold-chain/coop governance/D2C/adulteration+route jobs/Lactoscan/bonus-slabs/bank-disbursement payout deferred) |
| 17 | equipment | 3 | ✅ (CHC rental marketplace: equipment assets + per-asset rate cards + rental bookings (requested→quoted→confirmed→in_progress→completed→settled). ESCROW money path LIVE: confirm holds advance (renter→Escrow, escrow_hold); settle releases to owner + collects shortfall / refunds unused hold (all zero-sum, idempotent, Law 2); float-free total = rate×actual; OTP-gated start (HMAC + constant-time); actual≤estimate anti over-billing; `equipment` flag + equipment.manage/equipment.rent perms; confirm-timeout job. DRONES (DGCA)/maintenance logs/GPS-trace billing/operator-pool/payments-payout deferred) |
| 18 | warehousing | 3 | ✅ (WDRA storage + eNWR: warehouses (incl. platform-global/independent, cross-tenant visible) + storage bookings (requested→confirmed→stored→released) + accredited assays + electronic NWR issuance/release. Storage-fee settlement LIVE: depositor userMain → operator userMain, txnType storage_fee, zero-sum + idempotent (Law 2), float-free fee = qty×rate×months; one-active-NWR-per-booking; `warehousing` flag + warehouse.manage/warehouse.store perms. NWR pledge→loan/partial/default (fintech)/mark-to-market/re-assay jobs/auto-valuation/NERL-CCRL API deferred) |
| 19 | contract-farming | 3 | ✅ (buyer↔grower contracts: templates (incl. platform-standard, cross-tenant visible) + contracts (draft→proposed→signed→active→fulfilled) + grower enrolment + milestones (geo-photo) + input advances + settlement. Money LIVE: buyer userMain → grower userMain, txnType contract_payment, zero-sum + idempotent (Law 2); advance disbursement + harvest settlement (FIXED price, float-free gross, auto-recovers advances, net=gross−recovered); `contract_farming` flag + contract.manage/contract.grow perms. floor/ceiling/formula pricing+quality slabs/tripartite financier/e-sign/negotiating-disputed states/reminder job deferred) |
| 20 | exports | 3 | ✅ (agri-export compliance workflow: exporter RCMC/IEC registrations + export shipments (draft→docs_in_progress→inspection→shipped→delivered→paid→closed) + document checklist (pending→submitted→verified|rejected) + read-only compliance-rule browse. COMPLIANCE SHIP-GATE: cannot ship until every document verified (fail closed). NO in-platform money (LC/bank settlement external; total_value informational); `exports` flag + export.manage perm; seeds export_doc lookup + destination countries. compliance authoring (admin/Law11)/RCMC-expiry+reminder jobs/mandatory-doc templating/DGFT-ICEGATE/GI tagging deferred) |
| 21 | land-soil-weather | 3 | ✅ (farm-data backbone: land parcels (survey/Bhulekh, irrigation, area float-free ×10000) + crop seasons (planned→sown→harvested+abandoned, yields ×1000) + soil tests (SHC results) + read-only regional weather advisory browse (global/partitioned, ingestion deferred Law 11). NO money (agronomy data); `land_soil_weather` flag + land.manage perm; seeds irrigation + weather_alert lookups. weather ingestion/advisory-push+bhulekh jobs/parcel verification workflow/PostGIS/soil recommendation engine deferred) |
| 22 | fintech | 3 | ✅ (lending spine: partner + loan-product browse (global reference, read-only) + loan applications (submitted→under_review→approved→disbursed, +reject/withdraw) + loans (active↔overdue→closed) + repayments (partitioned). Money LIVE: disburse tenant main → borrower userMain (loan_disbursement) opens loan; repay borrower → tenant main (loan_repayment) closes at zero — zero-sum + idempotent (Law 2). Anti-predatory cooling-off gate + approved≤requested + over-repay reject. FPO-as-lender model; `fintech` flag + loan.borrow/loan.manage perms; seeds loan_kind + sample partner/product. credit-scoring/insurance/BNPL/SHG-groups/NWR-pledge/origination-fee/EMI-schedule/parametric/partner-rail-disbursement + jobs deferred (RBI/partner-gated)) |
| 23 | schemes | 3 | ✅ (govt-scheme application engine: scheme + authority browse (global reference) + explainable eligibility checker (deterministic rule eval) + applications (draft→submitted→under_verification→clarification↔→approved→disbursed→closed, +reject/appeal) with partitioned audit trail + observed DBT-credit records (no wallet movement; PFMS external). Money: optional processing fee on submit (applicant userMain → tenant main, service_fee, zero-sum + idempotent, Law 2); scheme rule-version snapshot integrity; `schemes` flag + scheme.apply/scheme.process perms; seeds scheme_category + sample authority/schemes. scheme/authority authoring (admin/Law11)/PFMS-sync+rule-refresh+escalation jobs/AI confidence/rule-DSL deferred) |
| 24 | communication | 4 | ✅ (M13 NOTIFICATION SPINE: global event catalog + per event×channel×language templates (+ tenant overrides, {{var}} render) + user preferences + quiet hours + partitioned delivery log w/ cost tracking + the domain-event→notification FANOUT — one outbox consumer per mapped event type (order/offer/quote/shipment/payment/dispute) renders + dispatches via an EXTERNAL notifier GATEWAY PORT (resilience-wrapped, degrade-not-die fallback; HTTP adapter by config else noop) inside the relay tx. Channel resolution = default_channels ∩ prefs, mandatory events ignore opt-out + critical bypasses quiet hours (DST-correct via Intl); deterministic notification id ⇒ idempotent re-delivery + gateway dedup; inbox list/mark-read (404 non-owner, no IDOR); HMAC delivery-status webhook; notification.manage template authoring. `communication` flag + seeds extend 0007 catalog/templates. PLUS the MESSAGING vertical: conversations (order/dispute/direct… + participants) + messages (append-only, partitioned, AI-badge + abuse-flag) + masked privacy-proxy calls via a MASKING_PROVIDER port (resilience-wrapped; stores only user ids + provider ref + duration, NEVER raw phones) with an HMAC call-status webhook. Membership-gated (non-participant ⇒ 404, no IDOR; participants joined to conversations so no cross-tenant leak); locked threads refuse messages; a posted message emits comm.message_posted → the SAME fanout raises a chat.message_posted alert. message.moderate perm for lock/unflag. Digest-batching + failed-retry poller + IVR/voice + recording retrieval deferred. **API-W10 broadcast ✅**: `POST/GET communication/broadcasts` (notification.manage, Idempotency-Key) — a tenant→audience announcement recorded in `tenant_broadcasts` (migration 0048, RLS) 'queued' + ONE outbox event; the BroadcastRequestedHandler resolves the audience (all active members, or one role) in KEYSET PAGES (Law 8) and fans each page through THIS notification spine (per-user prefs + quiet hours + deterministic dedup), then marks the broadcast sent — async, no synchronous blast (Law 4). Seeds the `tenant.broadcast` event + push/inapp templates) |
| 25 | education | 4 | ✅ (M09 agri-learning: instructor profiles (royalty_bps default 80%) + course authoring/lifecycle (draft→review→published↔paused→archived, state machine) + lessons (video/pdf/quiz…, ordered, idempotent upsert) + enrollment (free instant; PAID = zero-sum idempotent course_purchase wallet split learner→instructor royalty + platform Fees, bigint minor units, key coursebuy:<id>) + lesson progress → progress_pct recompute → completion stamp. authz throws (own-course edit/publish-gated, 404 non-owner, royalty not client-settable); courses/instructors allow platform-library NULL tenant; lessons/progress gated via course/enrollment JOIN; keyset lists. `education` flag + course.author/course.publish perms + seeds course_purchase ledger type + course_topic lookups. Certificate PDF issuance + online payment-intent enrol path + payout-aggregation jobs + quiz auto-grading deferred. PLUS CREATOR-CONTENT layer (migration 0027): external content channels (youtube/website/… register→pending, tenant moderator approve/suspend/reject = host-authority gate, audited) + curated resources (video/blog/post; auto-approved under own approved channel else moderated; takedown) + live streaming sessions (scheduled→live→ended/cancelled via resilience-wrapped STREAM_PROVIDER port, noop else HTTP; learner registration). Own-channel/own-session authz throws (404 non-owner), unapproved channel can't publish/stream (fail-closed), URL-validated (anti-SSRF), RLS-isolated. channel.host + content.moderate perms) |
| 26 | cms | 4 | ✅ (PRD §50 CMS: VERSIONED content pages (cms_pages, UNIQUE tenant+slug+version; draft→published→archived state machine; draft-only edit, publish stamps published_at + archives the slug's prior published version = single-live, re-edit mints a new version so live content is immutable; platform pages NULL tenant read-only Law 11) + public getBySlug serving latest published + scheduled banners (placement + [starts,ends) window, is_active, isLive, atomic click_count increment) + banner-expiry worker job. cms.manage-gated authoring (audited); kebab slug + http(s) URL validation (anti-SSRF); RLS-isolated (pages allow platform NULL); keyset lists. `cms` flag + cms.manage perm. Page translations + banner audience-rule targeting + preview tokens + scheduled publish deferred) |
| 27 | support | 4 | ✅ (PRD §50 helpdesk: support tickets (multi-channel, severity P0-P3 → SLA first-response + resolution due derived at open) with the ticket_status machine (open↔pending_customer↔pending_internal↔escalated→resolved→closed, +reopened) + agent workflow (assign/respond=first-response stamp/transition, support.handle-gated + audited) + requester CSAT (1-5, resolved/closed only). DisputeEscalatedHandler auto-opens a P1 ticket on disputes.dispute_escalated (idempotent by deterministic ticket_no); SLA-breach worker job escalates overdue tickets (FOR UPDATE bounded, BYPASSRLS pool). Reads owner-or-agent (404 IDOR, queue/assigned agent-only); RLS-isolated; open idempotent. `support` flag + support.handle perm + ticket_category seed. Threaded replies (link to conversations) + CSAT-survey dispatch + auto-routing + KB deflection deferred) |
| 28 | ambassadors | 4 | ✅ (village field-agent / referral growth engine: admin-enrolled ambassador profiles (tier/mentor/cluster, Law-11 not self-grant, audited) + commission plans (7 seeded streams as data, compute flat/rate×base capped, tenant-override→platform resolve) + generic referrals (invited→signed_up→activated→rewarded, self-referral + double-use blocked) + partitioned ambassador_earnings ledger (idempotent accrual via existsFor since UNIQUE includes partition key). Accrual: referral activation → farmer_onboarded (₹25), orders.order_completed handler → first_sale_facilitated (rate capped) for the referring ambassador. Money: weekly payout settles unpaid earnings → ONE zero-sum idempotent `commission` wallet transfer platform Fees → ambassador userMain (Law 2/3/4), FOR UPDATE SKIP LOCKED batch job. `ambassadors` flag + ambassador.manage perm + ambassador_tier seed. **API-W9 field-ops:** assisted-onboarding (`POST ambassadors/assisted-onboarding` — active-ambassador, DPDP-consent-gated, idempotent; composes identity UserService.adminCreate + ConsentService + a signed_up attribution referral; commission still accrues only on admin activation, no self-grant), geo visit-log (`POST/GET ambassadors/visits`, owner-scoped keyset), leaderboard (`GET ambassadors/leaderboard` — ranked by earned minor, pure competition-rank ties), targets (`POST ambassadors/targets` admin-set + `GET ambassadors/targets/me`); new tables ambassador_visits + ambassador_targets (migration 0047, RLS re-run). Milestone/inactivity jobs + AePS/kiosk + stipend disbursement + tier auto-promotion + sale_trail + on-behalf listing/order deferred) |
| 29 | market-intel | 4 | ✅ (PRD §16.2 Mandi Pulse: mandi registry browse (global ref) + price-observation log (mandi_prices, GLOBAL partitioned, append-only, bigint min≤modal≤max, sources agmarknet/enam/platform_txn/ambassador) + fair-price bands (price_predictions, GLOBAL partitioned, deterministic baseline P10/P50/P90 nearest-rank percentiles float-free, model baseline-v1) + per-farmer price alerts (TENANT-scoped, RLS, above/below threshold). Ingest (market.manage, idempotent) evaluates active alerts + emits PriceAlertTriggered per crossing (FOR UPDATE SKIP LOCKED, bounded) → notification spine; Mandi Pulse composite read (latest+band+history, CQRS replica). Partition-prune by product_id, keyset lists; alert reads owner-scoped 404 IDOR. `market_intel` flag + market.manage perm + sample mandi seed. Agmarknet/eNAM ingest jobs + platform_txn aggregation + external ML model + search synonyms deferred. **API-W11 name-join ✅**: `GET market/pulse` + `GET market/prices` enrich each row with productName/gradeName/regionName via `MarketNamesReadModel` (bounded id-set resolve from GLOBAL products/attribute_options/admin_regions, replica, Law 12; unknown id → null, degrade-never-throw) — no migration. Geocoded weather forecast still FLAGGED (external IMD/provider, uncontracted)) |
| 30 | traceability | 4 | ✅ (PRD §16.3 farm-to-fork QR: trace_lots (tenant-scoped, qr_token public capability, declared inputs/certs, blockchain_anchor) + trace_events (PARTITIONED, append-only TAMPER-EVIDENT hash chain event_hash=sha256(prev‖lot‖code‖meta), genesis off lot id; FOR UPDATE on lot keeps the chain head stable) + PUBLIC unauthenticated QR scan via SECURITY DEFINER trace_scan() function (migration 0028 — the single controlled RLS-bypass; REVOKE PUBLIC + GRANT kv_app; curated NON-PII projection, no tenant_id/user id) flag-gated fail-closed + anchor-hashes worker job (chain-head anchor). READ/scan is UNIVERSAL (no role, anonymous-public); only AUTHORING is gated — trace.manage held by lot-owning producer/seller roles farmer/pashupalak/dairy_farmer/vyapari/organic_store/pharma_store/fpo_coordinator (+tenant_admin), least-privilege, 404 IDOR. keyset + partition-prune. `traceability` flag + trace.manage perm. Order/shipment auto-fanout (payloads lack listing_id; appendForListing hook ready) + on-chain anchoring + recall workflow deferred) |
| 31 | ai-governance | 4 | ✅ (PRD §8.3 control plane for every AI decision: ai_models (GLOBAL registry, lifecycle shadow→canary→production→retired state machine, confidence_threshold) READ-ONLY here — lifecycle authoring is admin-api (Law 11), not on the tenant API; ai_inferences (tenant-scoped, append-only, PARTITIONED by created_at, bigserial composite PK) the explainability audit log — input_ref is POINTERS ONLY (domain rejects PII keys), human override stamped; ai_review_queue (tenant-scoped HITL, pending→in_review→accepted|rejected state machine) reviewer claims FOR UPDATE (no double-claim) + resolves, decision drives originating module via outbox, rejected→marks inference overridden; moderation_reports (tenant-scoped, open→actioned|dismissed) any user files (no perm) / moderators handle. Recording emits NO event (scale); enqueue/resolve/moderation do. Migration 0029: keyset+claim indexes + partial-UNIQUE moderation dedup (one live report per reporter+subject, ON CONFLICT — abuse guard §4). drift-watch + fairness-audit-monthly BYPASSRLS jobs. `ai_governance` flag + ai.review (ops) / content.moderate perms; audit on every reviewer+moderator action; keyset + partition-prune; cross-tenant RLS proven. Auto-recording from other modules' events + model lifecycle writes (admin-api) + worker registration of jobs deferred) |
| 32 | tenancy | 1* | ✅ (SaaS plans (global, plan.manage god-mode) + plan_limits + tenant subscriptions; active subscription drives core QuotaService (proven), GET /subscriptions/current dashboard, grace-period expiry job; `tenancy` flag. **Self-serve ✅ API-W3-05**: in-tenant profile edit (status/slug/risk NEVER writable) + onboarding submit + custom domains (verified-before-primary) + typed tenant-scoped settings (platform keys refused, Law 11) + read-only features/usage — all scoped to ctx.tenantId (no IDOR); god-mode lifecycle stays in admin-api tenant-ops. **SaaS invoicing ✅ API-W3-06**: renewal run raises+issues saas_invoices (gap-free no., idempotent/period), payments.payment_succeeded→paid (idempotent relay handler), trial-expiry + usage-limit-alert + overdue worker jobs; invoice state machine mirrors billing-ops; NO money moves here (collection/void/adjustment/dunning escalation are god-mode billing-ops, which reads these). Auto-renew charge + invoice PDF deferred. **API-W10 analytics ✅**: `GET tenancy/analytics?from&to` — the tenant's OWN dashboard (tenant.settings; never cross-tenant — that's admin-api platform-reports, Law 11), float-free SQL over a bounded window (default 30d, ≤366d; partition-pruned orders): gmv/orders/commission/platform-fee/refunds/active-listings/open-disputes/payouts-paid + top-5 products/sellers; view-traffic omitted (event pipeline, later wave), never faked) |
| 33 | services-marketplace | 3 | ✅ (P2P service marketplace: provider offerings (draft→published→paused↔published→archived, six pricing models, bigint minor units) + bookings (requested→confirmed→in_progress→completed, +cancel) with the fee SNAPSHOTTED from the offering at request (per_person × guests, exact bigint; never client-supplied) and the provider JOINed from service_offerings (not a column → payee/authz resolved server-side, anti-IDOR). Money: customer completes-and-pays → zero-sum + idempotent service_fee transfer customer userMain → provider userMain in-tx (Law 2/4), key svcbook:<id>; quota on create; keyset lists + FOR UPDATE; `services_marketplace` flag + service.offer/service.book perms; reuses seeded service_fee ledger type + global categories. Availability calendars/slot capacity/commission split/booking disputes/provider reviews/online prepay deferred) |

## Apps / surfaces
| Surface | Status |
|---|---|
| apps/api (modular monolith) | ✅ (all **38 modules wired in app.module + compiling — every module's PRIMARY flow is live**. **WAVE 3 (secondary sub-domains) COMPLETE — all of API-W3-01…W3-11 done**: catalogue (EAV + certificates/regulated-rules), logistics (fleet + zones/routes/cold-chain), tenancy (self-serve + saas-invoicing), payments (trade-invoice/settlement-statement/commission/tax/charge + payout-batch + recon/webhook), disputes (returns/RMA + SLA jobs), orders (cart-item/checkout-group/order-item + stats + publisher), and **auctions ✅ API-W3-11** (watch-list + outbid notifications + scheduled-edit + losers-EMD-release job + winner-EMD-on-payment handler). Every domain module is now built bar its **e2e-spec placeholder (W5)**. **WAVE 4 (async glue) COMPLETE — API-W4-01 done**: the last handlers/publishers/jobs across promotions (coupon-redemption recorder + order-created backstop + budget-watch/festival-scheduler sweeps), requirements (buyer-edit PATCH + listing-published match handler + need-by reminder job), reviews (booking-completed eligibility + review-prompt job), memberships (gateway payment→confirm handler) and ai-governance (typed AiGovernancePublisher façade) are filled, idempotent, registered in each module's OnModuleInit (outbox handlers) / instantiated by apps/worker (the 4 Pool sweep jobs); notification fan-out wired (map + seeded templates) for outbid/match/reminder/review-prompt/membership; migration 0044 adds the reminded_at/prompted_at idempotency markers. **WAVE 5 (test depth) COMPLETE — API-W5-01 done**: the e2e placeholders (identity/catalogue/orders/payments/auctions + smoke) are now REAL HTTP-through-the-app suites — a `test/e2e/bootstrap.ts` boots the full AppModule over the integration test DB, supertest-driven, real TokenService minting; a jest `e2e` project runs them in the CI DB job (describe.skip otherwise). They assert health 200, full OTP→token→/users/me login, auth-401, RBAC-403, feature-flag 404-invisible / 200-reachable, and tenant-scoped 404 (no IDOR) — closing DoD §7. **apps/api is COMPLETE — all of API-W3 + W4 + W5 cleared; every domain module + its async glue + e2e contract is built and green.** **CLIENT-UN-FLAG WAVES API-W1…W12 COMPLETE** (the post-GA fast-follows that turn client "coming soon"s live): wallet read-model, KYC doc-types, checkout-preview + pay-from-wallet, push-device registration, listing boost-price + analytics, buyer saves + public seller-profile + media URLs, auctions my-bids + EMD, labour apply/skills/attendance/lookups (0046), ambassador assist/visit-log/leaderboard/targets (0047), tenant analytics + broadcast (0048), market commodity-name join, and **API-W12** farmer DPDP export/deletion + change-phone (OTP) + tip-wishlist. Genuinely-external items remain explicitly FLAGGED not faked: Aadhaar-eKYC provider, geocoded weather forecast, farmer AI assistant (ai-services governed inference), dedicated cross-entity OpenSearch search, per-impression listing "views".) |
| apps/admin-api (god-mode plane, Law 11) | ✅ (foundation live: AdminConfig fail-closed boot + kv_admin pool + self-contained HS256 admin-JWT auth + owner-role RBAC + FIDO2/step-up/IP-allowlist guards + in-tx admin audit + access interceptor + bootstrap. First ops module: **ai-models-ops** ✅ — the GLOBAL ai_models lifecycle WRITE path (register/promote shadow→canary→production→retired/retire + threshold tuning + fairness reports), owner-only (ai.model.manage/read, NOT assignable to tenant roles), mutations require hardware-key + step-up, audit-in-tx, UNIQUE(code,version) 409, FOR UPDATE concurrency. Second ops module: **tenant-ops** ✅ — the god-mode tenant LIFECYCLE write path (approve pending/trial→active+stamp approved_at / suspend live→suspended / archive→archived via the authoritative tenant_status state machine, Law 5) + per-tenant numeric **limit overrides** (migration 0032 tenant_limit_overrides, bigint -1=unlimited; QuotaService read-merge in apps/api) + tenant **search** (keyset) + **scorecard** read. New owner perms tenant.manage/tenant.read + roles platform_tenant_ops/platform_tenant_viewer (NOT assignable to tenant roles, Law 11). Every mutation: owner-perm + FIDO2 hardware-key + step-up elevation; one ACID tx (lock FOR UPDATE → state machine → UPDATE tenants → tenant_status_events row → audit_log row, atomic); illegal transition throws + writes nothing; missing tenant→404; reason mandatory; zod .strict; keyset/bounded. 28 admin unit tests green (ai-models-ops + tenant-ops: state machine, entity guards, owner-RBAC no-escalation, DTO validation, audit-in-tx ordering, 404) + real-Postgres integration spec (approve→suspend→override→illegal-transition, gated on DATABASE_ADMIN_URL). Third ops module: **recon-monitor** ✅ — the god-mode MONEY-SAFETY plane (Law 11 + Law 9), closest to the ledger. READ-ONLY wallet reconciliation dashboard (latest reconciliation_run per type + ledger **zero-sum** health = SUM(ledger_entries.amount_minor) must be 0, balances as STRING minor units Law 2), mismatch **investigations** (state machine open→investigating→resolved|false_positive, one-open-per-run partial-unique dedup), and the emergency account **FREEZE** control. CRITICAL: it NEVER posts to the ledger — money moves only via the wallet-service (Law 2); a freeze just flips wallet_accounts.is_frozen (the guard the wallet engine honours to reject debits) → zero-sum untouched. New owner perms recon.manage/recon.read + roles platform_recon_ops/platform_recon_viewer (Law 11, not tenant-assignable). Every mutation: owner-perm + FIDO2 hardware-key + step-up; one ACID tx (lock FOR UPDATE → state machine / freeze guard → write → audit_log row, atomic); illegal transition / double-freeze throws + writes nothing; 404 on missing run/account/investigation; reason mandatory; zod .strict; keyset/bounded. Migration 0033 (recon_investigations + account_freeze_orders — god-mode platform tables, no tenant_id/RLS, kv_admin-only). 44 admin unit tests green (ai-models-ops + tenant-ops + recon-monitor: investigation state machine, freeze guards + NO-ledger-write assertion, owner-RBAC no-escalation, DTO validation, audit-in-tx, 404) + real-Postgres integration spec (open investigation+dedup; freeze→unfreeze asserting is_frozen + account_freeze_orders + audit + ZERO new ledger_entries; gated on DATABASE_ADMIN_URL). Fourth ops module: **compliance-ops** ✅ — the god-mode DPDP/COMPLIANCE plane (Law 11). Five surfaces: (1) DPDP **data-subject-request queue** over data_subject_requests (0003) — state machine open→in_progress→completed|rejected, with an **erasure cannot complete during the 90-day cooling window** guard; (2) **data-export approval gate** over data_export_jobs (0015 + 0034 approval cols) — a tenant full-export / DPDP portability bundle stays approval_status='pending' until platform compliance approves (worker claims only approved), pending→approved|rejected no-re-decide; (3) read-only **audit-log explorer** over the partitioned audit_log — keyset over (created_at,id) PARTITION-KEY-first for prune (Law 8), metadata only (no old/new value blobs); (4) **retention-policy admin** over data_retention_policies (0015) — bounds-validated upsert, audited; (5) DPDP §8 **breach-response console** over data_breaches (0034) — open→contained→notified→closed, notify requires BOTH regulator + data-principal timestamps, stores affected-data CATEGORIES not raw PII. New owner perms compliance.manage/compliance.read + roles platform_compliance_ops/platform_compliance_viewer (DPO/auditor; Law 11, not tenant-assignable). Every mutation: owner-perm + FIDO2 hardware-key + step-up; one ACID tx (state machine/guard → write → audit_log row, atomic); illegal transition / already-decided / cooling-active throws + writes nothing; 404s; zod .strict (audit filters regex/ReDoS-safe); keyset/bounded; mandatory reason. Migration 0034 (data_breaches god-mode table + data_export_jobs approval columns). Fifth ops module: **billing-ops** ✅ — the god-mode SaaS-BILLING plane (Law 11 + Law 2/9), the platform revenue stream over plans/subscriptions/saas_invoices (0002). Four surfaces: (1) **SaaS-invoice admin** — state machine draft→issued→paid|partially_paid→overdue→void (Law 5), only issue/mark_overdue/void are admin-settable (paid arrives from payment reconciliation, never a manual mark); (2) **dunning** over saas_invoice_dunning_attempts (0035) — append-only follow-up history (email/sms/whatsapp/call/in_app + outcome), invoice locked FOR UPDATE so attempts serialise, domain caps total attempts (abuse guard), bumps a denormalised dunning_attempts counter; (3) read-only **revenue dashboard** — MRR (active subs normalised to monthly, **annual ÷ 12 via integer division, never a float**), ARR = MRR×12, outstanding receivables, collected-in-window, invoice counts by status, all money bigint minor units as strings; (4) **manual money adjustments** over billing_adjustments (0035) — goodwill credit (platform→tenant: +tenant.main / −platform.promo_liability) or clawback debit (mirror), legs ZERO-SUM, posted by the **wallet-service over gRPC** (ledger_txn_type='billing_adjustment'). CRITICAL (Law 2/9): billing-ops NEVER writes ledger_*/wallet_accounts — money moves ONLY via the wallet-service through the WALLET_ADMIN port (core/wallet/wallet-grpc.client.ts, fail-closed if WALLET_GRPC_ADDR unset, hard RPC deadline); the adjustment is idempotent per (tenant,key) at both the admin record (UNIQUE idempotency_key) and the wallet post — replay never double-moves money; a wallet failure writes an audit 'failed' entry + returns a typed 502 with NO local row (clean retry). New owner perms billing.manage/billing.read + roles platform_billing_ops/platform_billing_viewer (finance/revenue; Law 11, not tenant-assignable). Every mutation: owner-perm + FIDO2 hardware-key + step-up; one ACID tx (state machine/validate → write → audit_log row, atomic); illegal transition / non-dunnable / over-cap throws + writes nothing; 404s; zod .strict (money as digit-string→bigint, rejects float/negative); keyset/bounded; mandatory reason. Migration 0035 (billing_adjustments + saas_invoice_dunning_attempts — tenant_id + FORCE RLS; saas_invoices dunning counter cols) + seed ledger_txn_type 'billing_adjustment'. 80 admin unit tests green (5 modules: invoice state machine + entity, dunning cap, adjustment money domain — balanced zero-sum bigint legs, float-free revenue math, owner-RBAC no-escalation, DTO validation incl. float/negative rejection, services audit-in-tx + 404 + money-only-via-wallet + idempotent replay + no-row-on-wallet-failure) + real-Postgres integration spec (invoice draft→issued→overdue + dunning counter/audit; manual adjustment record + audit + idempotent replay via a fake wallet port; gated on DATABASE_ADMIN_URL). Sixth ops module: **flags-ops** ✅ — the god-mode FEATURE-FLAG control plane (Law 10 + Law 11), the only authorised WRITER of the GLOBAL feature_flags table (0002) that apps/api core/feature-flags READS. Owns: the flag **registry** (create — always default OFF, Law 10; keyset browse over (created_at,key); single + history reads); **percent rollout + targeting** (deterministic 0..100 + tenant_ids/plans/countries allowlist, validated/bounded, persisted in the snake_case `rules` shape the runtime reads); on/off + the emergency **KILL-SWITCH** (kill = disable for everyone AND set is_locked=true so no operator can re-enable/re-ramp/re-target until an explicit unlock, which does NOT re-enable). domain/rollout.ts mirrors the runtime evaluator byte-for-byte (FNV-1a bucket, allowlist-then-percentage) so the console previews who a flag is on for (unit-tested parity). New owner perms flags.manage/flags.read + roles platform_flags_ops/platform_flags_viewer (SRE/release; Law 11, not tenant-assignable). Every mutation: owner-perm + FIDO2 hardware-key + step-up (a global flip affects every tenant); one ACID tx (lock FOR UPDATE → entity guard → UPDATE feature_flags → feature_flag_changes row → audit_log row, atomic); a locked-flag re-enable / bad rollout / unlock-when-unlocked throws + writes nothing; unknown flag→404; reason mandatory; zod .strict discriminated-union DTO; targeting arrays size-capped + charset-validated; keyset/bounded. Migration 0036 (feature_flags.is_locked kill-switch lock + feature_flag_changes append-only history — god-mode tables, no tenant_id/RLS, kv_admin-only). 102 admin unit tests green (6 modules: flag entity + kill-switch lock, rollout/targeting validation, evaluator parity, owner-RBAC no-escalation, DTO validation, services change-row + audit-in-tx + lock-enforcement + 404) + real-Postgres integration spec (create→enable→ramp→kill asserting state + lock + locked-refuses-re-enable + feature_flag_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). Seventh ops module: **plans-ops** ✅ — the god-mode SaaS PLAN-CATALOGUE plane (Law 11), authoritative WRITER of the GLOBAL plans/plan_features/plan_limits catalogue (0002) that billing-ops (subscriptions→saas_invoices) and the tenant QuotaService (plan_limits via active subscription) consume. Surfaces: (1) **plan CRUD + lifecycle** — create DRAFT (not sellable) → publish→active / archive→archived / reactivate via the state machine (Law 5, migration 0037 plans.status; is_active kept in sync; auto next version per (code,country)); (2) **pricing + versioning** — setPrices on a DRAFT only; a published plan's prices are IMMUTABLE (grandfathering — existing subs keep their version), changed by `version` which clones to a new DRAFT version+1 with new prices + cloned features/limits (isPublic=false ⇒ private/anchor deal); (3) **feature + limit assignment** over plan_features/plan_limits (limit_value bigint, -1=unlimited, the value QuotaService enforces) — editable DRAFT-only, feature code FK-checked against the features catalogue (typed 404). All prices bigint minor units (Law 2, zod digit-string→bigint, never float). New owner perms plans.manage/plans.read + roles platform_plans_ops/platform_plans_viewer (pricing/product; Law 11, not tenant-assignable). Every mutation: owner-perm + FIDO2 hardware-key + step-up; one ACID tx (lock FOR UPDATE → entity guard → UPDATE → plan_changes row → audit_log row, atomic); illegal transition / immutable edit / unknown feature / (code,version,country) conflict throws + writes nothing (409/404/422); reason mandatory; zod .strict; keyset/bounded. Migration 0037 (plans.status draft/active/archived + plan_changes append-only history — god-mode tables, no tenant_id/RLS, kv_admin-only). 121 admin unit tests green (7 modules: plan lifecycle + entity price-immutability, validation helpers, owner-RBAC no-escalation, DTO validation, services audit-in-tx + version-409 + illegal-transition/immutable/unknown-feature/404 + draft-only composition) + real-Postgres integration spec (create→compose→publish→version→archive asserting state + composition + immutability + v2 clone + plan_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). Eighth ops module: **impersonation** ✅ — the HIGHEST-SENSITIVITY control (god-mode act-as, Law 11), built deliberately safe-by-construction. A support operator acts AS a tenant user via a SHORT-LIVED, READ-ONLY, TIME-BOXED grant with a mandatory reason; EVERY action under it is recorded. The act-as TOKEN (domain/impersonation-token.ts, dependency-free HS256) is `typ='impersonation'` (NEVER 'access'), signed with a DEDICATED key (IMPERSONATION_TOKEN_SECRET, not the admin/user secret), carries the RFC-8693 `act` (actor=admin) claim + grant jti + `scope='read_only'` + short exp, and is returned ONCE — an un-upgraded apps/api CANNOT accept it (different typ+secret) ⇒ fail-closed (the apps/api honour-hook is explicitly flagged in the README, not faked). Safety gates on start: kill-switch (Law 10, IMPERSONATION_ENABLED default OFF) → read-only scope only (write/full excluded) → no self-impersonation → ttl ≤ hard cap (IMPERSONATION_MAX_TTL_SEC=30min) → target must be an active member of THAT tenant (else 404, no enumeration) and NOT hold any roles.scope='platform' role (else 403 — never act as staff/god). Grant lifecycle active→ended|expired|revoked (Law 5); one active grant per (operator,target). New owner perms impersonation.grant/impersonation.read + roles platform_support_impersonator/platform_impersonation_auditor (NEVER '*', never money/god — unit-tested). Start/end/revoke require owner-perm + FIDO2 hardware-key + step-up; one ACID tx + audit_log row; recordAction logs every request into impersonation_actions and refuses a foreign/closed/expired grant (404). Migration 0038 (impersonation_grants + impersonation_actions — god-mode tables, target_tenant_id not tenant_id ⇒ no RLS, kv_admin-only). 140 admin unit tests green (8 modules: grant state machine + entity, scope/ttl/self guards, token round-trip + tamper/expiry/secret/typ + never-access, owner-RBAC no-escalation + no-'*', DTO validation, services kill-switch + 404-non-member + 403-privileged + audit-in-tx + illegal-transition + recordAction-refuses-stale) + real-Postgres integration spec (start→record→end vs a real tenant member, asserting state + time-box + actions + audit + re-end-refused; gated on DATABASE_ADMIN_URL). Ninth ops module: **support-oversight** ✅ — the god-mode CROSS-TENANT support NOC (Law 11) over support_tickets (0012). Read surfaces: the ticket queue (filter tenant/status/severity/assigned/SLA-breached), the **SLA-breach queue** (P0-first then oldest), a single ticket with computed SLA state, and **per-tenant health** (open/breached/P0-open counts + oldest-open age; tenantId ⇒ that tenant, else top tenants by open breaches). Cross-tenant by design — admin-api's kv_admin BYPASSES RLS (the integration test proves a breached ticket is visible with NO tenant context set); every read keyset + bounded (max LIMIT 100). domain/sla.ts mirrors apps/api's SLA_MINUTES (P0 15m/4h … P3 8h/72h) so the breach view matches what the tenant plane computed at open. The one consequential WRITE — **escalate** (raise severity RAISE-only, move to 'escalated' via the ticket state machine Law 5, recompute the SLA clock, optional reassign to a validated platform user) — needs support.oversight.manage + FIDO2 + step-up; one ACID tx + audit_log row; a resolved/closed ticket can't be escalated, a downgrade/no-op is rejected (422), a missing ticket/assignee → 404. New owner perms support.oversight.read/support.oversight.manage + roles platform_support_oversight/platform_support_oversight_viewer (NEVER '*', never money/god — unit-tested). No new migration (reads existing support_tickets; audits to audit_log). 154 admin unit tests green (9 modules: SLA math, ticket state machine, escalate entity raise-only/state-machine/recompute/no-op/closed-guard, owner-RBAC no-escalation + no-'*', DTO validation, services audit-in-tx + 404 + computed-SLA reads) + real-Postgres integration spec (cross-tenant breach visibility without tenant context + tenant-health count + escalate raises severity/status + audit; gated on DATABASE_ADMIN_URL). Tenth ops module: **platform-reports** ✅ — the god-mode READ-ONLY exec-dashboards plane (Law 11): cross-tenant rollups over EXISTING data (subscriptions/orders/tenants/login_events), no new schema, no writes, no money movement. Surfaces: **overview** (MRR from subscriptions with annual÷12 floored in SQL + ARR=MRR×12, active-tenant counts by tenant_status, active users + login-success bps over a window, headline GMV), **gmv** (SUM(orders.total_minor) + platform_fee + commission + AOV over a created_at window, optional tenant), **tenant-growth** (new tenants/month, ≤13 buckets), and a PII-free **regulator-export** snapshot. Cross-tenant via kv_admin (RLS bypassed) — the god-mode reporting plane; every money figure is **bigint minor units as a STRING** (SQL SUM(...)::text — never a float), ratios are **integer basis points**, windowed queries are bounded (≤366 days, validated → 422) and prune the partitioned orders/login_events tables (Law 8). New owner perm reports.read + role platform_reports_viewer (read-only; NEVER '*', never manage/money/god — unit-tested). Pure reads ⇒ no FIDO2/step-up (the @Global interceptor access-logs reads); no migration. 163 admin unit tests green (10 modules: float-free metric math + bps + AOV, window validation, owner-RBAC no-escalation + no-'*', DTO validation, services composing the read-model with money-as-string + PII-free export) + real-Postgres integration spec (every aggregate SQL executes cross-tenant + returns money-as-integer-string / counts-as-number shapes; gated on DATABASE_ADMIN_URL). Eleventh ops module: **providers-ops** ✅ — the god-mode INTEGRATION-PROVIDER registry plane (Law 11 + Law 12) over integration_providers (0002; Razorpay/RazorpayX/MSG91/NDDB/PFMS…). Reads: registry list/get (filter category/active) each with **credential-ref health** (per-provider counts of configured + active tenant_integrations), a **health monitor** rollup with a `degraded` flag (disabled-but-still-referenced), a **financial-partners** lens (payment+KYC categories), and change history. The one consequential WRITE — **enable/disable** a provider platform-wide (Law 12 degrade: pull a failing PSP/SMS/KYC provider out of rotation) — needs providers.manage + FIDO2 + step-up; one ACID tx (lock FOR UPDATE → entity toggle, no-op rejected 409 → UPDATE → provider_changes row → audit_log row); missing provider → 404; mandatory reason. CRITICAL (§4): tenant_integrations.secret_ref (the vault ARN) is **NEVER selected, mapped, or returned** — only COUNTS; the entity carries no secret field (unit + integration tested that the ARN never appears in any output). Cross-tenant health via kv_admin (RLS bypassed). New owner perms providers.manage/providers.read + roles platform_providers_ops/platform_providers_viewer (NEVER '*', never money/god — unit-tested). Migration 0039 (add_std_columns on integration_providers for who/when stamping + provider_changes god-mode history table). 175 admin unit tests green (11 modules: provider entity enable/disable + no-op 409, category validation, owner-RBAC no-escalation + no-'*', DTO validation, services audit-in-tx + 404 + degraded-flag + no-secret-leak) + real-Postgres integration spec (disable→enable a throwaway provider asserting is_active flip + provider_changes + audit + no-op-rejected; credential-ref health counts cross-tenant while secret_ref NEVER appears; gated on DATABASE_ADMIN_URL). Twelfth ops module: **announcements** ✅ — the god-mode platform-wide ANNOUNCEMENTS plane (Law 11): banners/notices (maintenance, incident, product) shown across tenant panels/apps, distinct from cms tenant `banners`. CRUD + schedule with a draft→scheduled→published→expired/archived lifecycle (Law 5); content is PLAIN TEXT (assertPlainText rejects `<`/`>` + control chars → stored-XSS closed by construction, §4); audience targeting ({plans,countries}, bounded); schedule windows forward/future/≤365d; content+schedule editable only while draft/scheduled (published is immutable — expire/archive + recreate); a `/active` read surfaces the currently-live set (published + within window), the same predicate the apps/api banner read path will use (flagged). New owner perms announcements.manage/announcements.read + roles platform_announcements_ops/platform_announcements_viewer (NEVER '*', never money/god — unit-tested). Every mutation: manage-perm + FIDO2 + step-up; one ACID tx (state machine → UPDATE → announcement_changes row → audit_log row); illegal transition/immutable-edit/bad-window throws + writes nothing; 404s; mandatory reason; keyset/bounded. Migration 0040 (platform_announcements + announcement_changes — god-mode tables, no tenant_id/RLS, kv_admin-only). 187 admin unit tests green (12 modules: lifecycle state machine, entity publish/expire/archive + edit-immutability, plain-text sanitisation, audience/window guards, owner-RBAC no-escalation + no-'*', DTO validation, service audit-in-tx + immutable-guard + 404) + real-Postgres integration spec (create→schedule→publish→expire asserting state + window + live-read + announcement_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). Thirteenth ops module: **global-catalogue-ops** ✅ — the god-mode PLATFORM MASTER-TAXONOMY plane (Law 11): the shared vocabulary every tenant's catalogue inherits, authored centrally. Two registries: (1) **controlled vocabularies** over lookup_types + PLATFORM lookup_values (0001) — register a vocabulary, add/rename/retire its PLATFORM values (ALWAYS scoped `tenant_id IS NULL`; a tenant's own values are never touched here, Law 11); `code` is the STABLE, IMMUTABLE reference key other tables FK by (e.g. certificates.cert_type_id), only default_name/meta/sort_order + is_active mutate; `meta` bounded (≤50 keys, ≤4 KB, primitives only); (2) the 5-level **category tree** over categories (0004, ltree path + depth 1..5) — create (code/path/depth derived from parent), rename + flag-edit (commerce_kind/requires_license/requires_certificate/min_age/sort_order/icon), **reparent (move)** a node + its whole subtree in ONE bounded ltree splice (path/code/depth recomputed for every descendant in a single UPDATE, no per-row loop), and activate/deactivate. Tree invariants enforced fail-closed: depth ≤ 5, no cycles (new parent ∉ {node∪descendants}), bounded subtree-move (write-amplification cap), no orphaning (can't deactivate with active children, can't activate under an inactive parent); never hard-deleted (deactivate retires a branch, keeping product FK refs valid). New owner perms catalogue.manage/catalogue.read + roles platform_catalogue_ops/platform_catalogue_viewer (NEVER '*', never money/god — unit-tested). Every mutation: manage-perm + FIDO2 + step-up (a taxonomy change ripples into every tenant); one ACID tx (lock FOR UPDATE → domain guard → write → catalogue_changes row → audit_log row, atomic); duplicate code / cycle / depth-overflow / oversized-move / parent-inactive / active-children / illegal no-op throws + writes nothing (409/422); unknown id/code → 404; display names PLAIN TEXT (assertPlainText rejects `<`/`>` + control chars → stored-XSS closed by construction, §4); codes/slugs charset-bounded ReDoS-safe; mandatory reason; zod .strict; keyset/bounded. Migration 0041 (catalogue_changes append-only history — god-mode table, no tenant_id/RLS, kv_admin-only — + admin keyset indexes on categories/lookup_values). Larger sibling surfaces (attributes/options, products master, synonyms, regulated-rules) are deferred to follow-on sub-modules, intentionally NOT stubbed (README). 219 admin unit tests green (13 modules: plain-text/code/slug/meta/sort guards, tree maths — derive code/depth + depth-limit + cycle detection + bounded-move + leaf-slug, entity mutation/no-op guards, owner-RBAC no-escalation + no-'*', zod .strict DTOs, services audit-in-tx + duplicate/404/parent-inactive/active-children/oversized-move fail-closed) + real-Postgres integration spec (lookup type+PLATFORM value asserting tenant_id NULL + retire; category create→child→move-to-root→deactivate asserting recomputed path/code/depth over the ltree splice + catalogue_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). Fourteenth ops module: **schemes-registry-ops** ✅ — the god-mode GOVERNMENT-SCHEME MASTER plane (Law 11): the authoritative WRITER of scheme_authorities (issuing bodies) + the code-keyed, versioned schemes catalogue (0011) that the apps/api schemes module reads READ-ONLY and snapshots (schemes.version → scheme_applications.scheme_version, rule-change integrity PRD R18). Surfaces: (1) **authority registry** — create/update (default_name/level central|state|district|body/region_id), never hard-deleted (schemes FK authority_id); (2) **scheme CRUD + lifecycle** — create (INACTIVE by default, fail-safe: a half-configured scheme isn't live until explicitly activated; code UNIQUE+IMMUTABLE), updateMeta (name/authority/category/source_url — NO version bump), activate/deactivate; (3) **eligibility-rules editor** — eligibility_rules/benefit_summary/required_doc_type_ids/applicable_region_ids/processing_fee_minor edits BUMP schemes.version IN PLACE (snapshot integrity — submitted applications keep their version), processing_fee_minor is **bigint minor units** (Law 2, digit-string→bigint, never float, emitted as string); (4) **window calendar** — set application_window ({opens/closes 'MM-DD', season?}, year-wrap aware, NO version bump) + a read-only "open on a date" calendar (defaults to today UTC). category_id is FK-checked against ACTIVE PLATFORM 'scheme_category' lookup values (→422); authority_id against scheme_authorities (→404); duplicate code →409; names PLAIN TEXT (no HTML/control), source_url must be http(s) (rejects javascript:/data:), JSON blobs bounded ≤8 KB non-empty, id arrays uuid-validated + capped (docs ≤100, regions ≤2000). New owner perms schemes.registry.manage/schemes.registry.read + roles platform_schemes_ops/platform_schemes_viewer (NEVER '*', never money/god — unit-tested). Every mutation: manage-perm + FIDO2 + step-up (a master edit ripples into every tenant's scheme catalogue); one ACID tx (lock FOR UPDATE → domain guard → write → scheme_registry_changes row → audit_log row, atomic); no-op/duplicate/bad-category/unknown throws + writes nothing; mandatory reason; zod .strict; keyset/bounded. Migration 0042 (scheme_registry_changes append-only history — god-mode table, no tenant_id/RLS, kv_admin-only — + admin keyset indexes on scheme_authorities/schemes). 245 admin unit tests green (14 modules: code/name/level/json/uuid/window/fee/url guards, entity mutation split — meta/window NO version bump vs rules BUMPS version — + no-op guards + fee-as-string, owner-RBAC no-escalation + no-'*', zod .strict DTOs, services FK-check + duplicate/category/404 fail-closed + audit-in-tx + version-bump + calendar default-date) + real-Postgres integration spec (authority→scheme(inactive,v1)→rule version-bump→window→activate against the seeded 'scheme_category' lookup, asserting version increment + fee as bigint + window/calendar surfacing + scheme_registry_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). Fifteenth ops module: **cells-ops** ✅ — the god-mode SHARD/CELL ROUTING-DIRECTORY plane (Law 8 + Law 11 + Law 12), the most infra-sensitive: the platform topology as data. Owns (migration 0043, all god-mode/no-tenant_id): **cells** (per-country independent stacks — the DPDP residency boundary; status active/draining/readonly/retired via the node state machine Law 5; one default landing cell per country via a partial unique index; a residency lock + soft tenant-capacity cap), **shards** (physical partitions the core/sharding ShardRouter hash maps to; dsn_secret_ref is a VAULT ref — NEVER selected/returned, toJSON exposes only hasDsn, change record masks it ***), **tenant_placements** (the authoritative tenant→cell+shard directory the edge gateway + core/cells cell-resolver read to route every request; keyed by `placed_tenant_id` NOT tenant_id — a GLOBAL directory read before tenant context exists, intentionally NOT RLS-scoped, no cross-DB FK), and **cell_map_changes** (append-only history). Three services: cell-registry (topology + status lifecycle; retire requires empty placed_count=0), tenant-cell-assignment (the FAIL-CLOSED routing-directory writes — place/move/remove with placed_count bookkeeping on both nodes in the same tx), data-residency-rules (per-country posture report + residency-lock toggle). Routing guards all THROW (a misroute = data on the wrong stack/country): target cell AND shard must be `active` (acceptsPlacement), shard must belong to the target cell, a capped cell may not be overfilled, double-place rejected; and the DPDP invariant — a residency-locked cell's tenants may NEVER move across a country border (ResidencyViolationError). New owner perms cells.manage/cells.read + roles platform_cells_ops/platform_cells_viewer (NEVER '*', never money/god — unit-tested); every mutation manage + FIDO2 + step-up; one ACID tx (lock FOR UPDATE → guards → directory write → counters → cell_map_changes + audit_log, atomic); zod .strict; keyset/bounded; mandatory reason; no money (capacity/weight are counts). apps/api integration note: ShardRouter (FNV-1a, config shard_count) + cell-resolver are the readers — authoritative-but-dormant at single-cell today, turning on N cells/shards is config+data not a rewrite (ADR-0007). 272 admin unit tests green (15 modules: node state machine legal/illegal/terminal, capacity/residency/accepts-placement guards, code/name/index/weight/dsn validation + dsn-never-leaks + raw-DSN-rejection, owner-RBAC no-escalation + no-'*', zod .strict DTOs, services fail-closed routing — already-placed/mismatch/non-active/over-capacity/cross-residency/no-op/404 — + retire-empty + audit-in-tx + counter bookkeeping) + real-Postgres integration spec (two same-country cells+shards → place → move-within-country → cross-residency move BLOCKED → remove, asserting placed_count bookkeeping + cell_map_changes timeline + audit rows; gated on DATABASE_ADMIN_URL). **All admin-api ops-module scaffolds are now ✅ — the god-mode plane is complete (15 ops modules + core foundation).**) |
| apps/wallet-service | ✅ | standalone money authority (Law 2) extracted from core/wallet: fail-closed WalletConfig + kv_wallet pg pool (bounded statement/lock timeouts) + ledger engine (zero-sum/single-currency/≥2-legs/idempotent UNIQUE key/FOR UPDATE deterministic lock order/no-overdraw/frozen-reject/sha256 hash-chain — identical formula to the in-process client) + bigint minor units end-to-end + HOT-ACCOUNT STRIPING (platform accounts spread across shard_no 0..N-1, deterministic stripe from idem key, balance summed across stripes — replaces shard-0 placeholder) + reconciliation (zero_sum + hourly_internal → reconciliation_runs) + gRPC transport (wallet.proto: PostTransaction/GetBalance, error→grpc status) + bootstrap. Core is framework-free + unit-tested (10 specs: invariants/striping) + real-PG integration (post→replay→Σ=0→recon); gRPC layer compiles under CI pnpm install. wallet tables intentionally outside RLS (kv_wallet-only). Razorpay(X) gateway integration stays in payments module (deferred move) |
| apps/web-storefront | ✅ (Next.js 14 App Router on the shared SDK — buyer storefront COMPLETE through Wave 5. **Quality (SF-W5-01 ✅):** ts-jest unit tests for the pure logic (discovery money math, auction BigInt bid math, order timeline, payment status), robots.ts + sitemap.ts + metadataBase (NEXT_PUBLIC_SITE_URL), 367-key i18n parity en/hi/gu with zero inline literals, root loading/error/not-found boundaries, a11y (html lang/dir + skip-link + landmarks + labelled controls), and the §4 self-audit green (no token/secret in any client component, server-only on every server lib, money only via formatMoneyMinor, no stray process.env). Verification gate (tsc/next lint/next build + jest) runs in CI's pnpm toolchain. Public pages: home (SSR listings browse), tenant storefront ([tenantSlug]) with **faceted discovery (SF-W2-01 ✅): search/saleType/organic/price-band/sort via features/discovery/query (float-free minor-unit string math), no-JS GET-form SearchFilters, keyset next-page preserving filters, shareable searchParam URLs, localized results-count/zero/empty states; categoryId/regionId honoured as URL passthrough — named category-nav/region/attribute facets flagged pending an SDK lookup resource**, **listing detail (SF-W2-02 ✅): enriched localized layout, seller trust card + listing reviews summary (public reviews.summary, degrade-silent), BuyerActions CTAs by sale type — add-to-cart (cart.addItem) + make-offer (offers.make, idempotency-keyed) as authed Server Actions (requireSession→serverClient, server-side validation, localized ?status notice), OpenGraph/Twitter/canonical metadata; media gallery / /trace deep-link / place-bid flagged pending listing read-model fields (media/qrToken/auctionId)**, PUBLIC farm-to-fork QR provenance (/trace/[qrToken], SEO, NON-PII). **App shell (SF-W0-01 ✅):** localized header (nav, locale switcher, cart badge, login) + footer; server-side i18n via lib/i18n + @krishi-verse/i18n Translator (en/hi/gu, 68-key parity, no inline literals); no-JS locale switch (/api/lang route, fail-closed + same-origin redirect guard); 6 marketing/static pages (/about /blog /help /pricing /press /tenants-signup — static copy, SDK has no cms resource) each with generateMetadata + segment loading/error; shared accessible DataTable (caption, keyset next); a11y skip-link + html lang/dir. **Notifications (SF-W4-03 ✅):** protected /notifications inbox (notifications.inbox keyset, unread filter, server-rendered payload title/body + same-origin deep link, per-item markRead Server Action) + /notifications/preferences (event×channel matrix full-replace setPreferences w/ mandatory-event error, quiet-hours get/set w/ time inputs + IANA tz, Asia/Kolkata default); header Notifications link when signed in. Authed, idempotent PUTs. **Auctions (SF-W4-02 ✅):** public /auctions browse (cards + live Countdown client, keyset) + /auctions/[id] (auctions.get+listBids+listing title, current high/min-next via pure BigInt helpers, reserve, bid history w/ no bidder PII + sealed handling, anti-snipe + EMD notes); authed placeBidAction (auctions.placeBid, randomUUID idempotency, parseMajorToMinor, EMD held server-side). Flag-gated env.featureAuctions (NEXT_PUBLIC_FEATURE_AUCTIONS → notFound + header link hidden when false); API auctions flag authoritative (degrade-empty). Watch flagged (no SDK method); EMD amount not in read-model. **Offers+messaging (SF-W4-01 ✅):** /offers buyer inbox (offers.list outgoing, keyset) + /offers/[id] (accept/counter[parseMajorToMinor]/reject Server Actions while open, converted-order link, message-seller→conversations.open via listings.get sellerUserId); /messages list + /messages/[id] thread (conversations.get+listMessages+auth.me bubble-align, markRead, post-message) + masked-call CTA (maskedCalls.initiate, callee userId derived from senders — NO phone shown). Authed + idempotency where SDK exposes; header Offers/Messages links when signed in. **Reviews-write (SF-W3-04 ✅):** /orders/[id]/review (protected, notFound on foreign id, completion-gated via pure orderTimeline) — accessible no-JS star radio + comment → submitReviewAction (reviews.create, randomUUID idempotency, server-side target/eligibility), one-per-order rule surfaced (duplicate rejected server-side), order-detail Write-a-review CTA + thanks notice. Seller-response-on-listing flagged (SDK reviews exposes only create + aggregate summary, no individual-review/response read). **Orders (SF-W3-03 ✅):** protected /orders (orders.list role=buyer, keyset via DataTable, localized status) + /orders/[id] (orders.get, notFound on foreign id/no IDOR, OrderTimeline from pure features/orders/timeline + cancelled/disputed terminal, line items + server-computed totals breakdown, shipment tracking via shipments.list degrade-silent); money formatMoneyMinor, dates formatDate. Invoice download flagged (no SDK invoice method). **Checkout+pay (SF-W3-02 ✅):** /checkout (address select + coupon + subtotal) → placeOrderAction (checkout.checkout, stable per-render Idempotency-Key) → /checkout/pay PayButton (real Razorpay: createOrderIntentAction reads authoritative order total + payments.createIntent direct_order under stable order-pay:<id> key → opens checkout with NEXT_PUBLIC publishable key → polls payments.get; capture webhook-verified server-side) → /checkout/confirm server-computed breakdown. Minor-unit strings via formatMoneyMinor; fail-closed (no key/failed/cancelled/pending); session token never in browser. Flagged: no coupon-preview/delivery-methods lookup (computed at placement); multi-seller pays primary + links to orders. **Cart (SF-W3-01 ✅):** protected dynamic /cart (requireSession), authed Server Actions update/remove/clear (serverClient().cart.*, revalidatePath, naturally idempotent), live totals via formatMoneyMinor (INR platform currency), priceChanged/unpurchasable warnings + checkout-gating, empty/error states; header async with live cart badge (features/cart/summary, anon/failure→0). **Auth (SF-W1-01 ✅):** phone-OTP login at /login — single loginAction Server Action (request→verify by intent) on anonymous publicClient().auth, enumeration-safe, randomUUID Idempotency-Key per call, OTP never rendered; tokens written to httpOnly+Secure+SameSite cookies (kv_session access + kv_refresh refresh) — browser never holds a token; lib/session silent refresh (refreshSession) + requireSession(returnTo) gate redirecting anon to /login?next=; same-origin safeNext guard; header shows logout vs login. Data layer: lib/env (single reader, no secrets in bundle, fail-closed, +tenantAppUrl), lib/api-client (server+public SDK factories), lib/auth (cookie helpers), lib/session (gate+refresh). Money via formatMoneyMinor (string minor units). Degrade-never-die public pages. Next/React + jest run in CI's pnpm toolchain; foundation packages verified offline. SDK-gap flags (deferred, documented in the backlog): categories/regions/attributes lookup, listing media/qrToken/auctionId, payments invoice, reviews list/seller-response, auctions watch) |
| shared web foundation (@krishi-verse/sdk-js + tokens + i18n) | ✅ | sdk-js: typed resilient API client (timeout+idempotent-GET retry, no mutation retry, bearer/tenant/idempotency headers, token never leaked, {data,meta} envelope, money=string minor units, typed SdkError; resources auth/listings/catalogue/traceability) — 7 unit tests. tokens: design palette/spacing/typography constants. i18n: language registry (hi/en/gu) + Intl formatters incl. formatMoneyMinor (bigint-string, no float, lakh/crore grouping) + Translator — 6 unit tests. All framework-free, typecheck + tests green offline |
| apps/web-tenant | ✅ (Next.js 14 App Router seller/tenant console on the shared SDK. TC-W0-01 foundation refit ✅: full i18n (en/hi/gu catalogs, full parity; server Translator off kvt_lang→Accept-Language→default; /api/lang same-origin-guarded cookie + LocaleSwitcher); app shell (layout sets html lang/dir + skip-link, Sidebar chrome gated on session cookie, nav links only to built routes, staff name from auth.me, sign-out); localized loading/error/not-found boundaries; async session+silent-refresh (lib/session: refreshSession off kvt_refresh, requireSession redirect to /login?next= via pure same-origin safeNext guard). Pages: phone-OTP login (Server Actions, enumeration-safe, same-origin next, httpOnly Secure SameSite=Lax tokens), requireSession-gated dashboard (auth.me), listings (SDK browse, keyset) + New-listing CTA, orders (typed orders.list({role:'seller'}) — request() escape hatch dropped, keyset), logout route. TC-W1-01 ✅: /listings/new draft creation — server-rendered searchable product picker (catalogue.browseProducts; option encodes productId+categoryId+defaultUnit), validated form, float-free major→minor money parse (features/listings/form.ts, unit-tested), listings.create via Server Action with the form's stable Idempotency-Key; real two-step media upload in one client island (MediaUploader: sha256+dims in browser → media.requestUpload Server Action → PUT bytes straight to presigned S3 (no token, never through our API) → media.confirmUpload → confirmed mediaIds submitted), fail-closed per-file. TC-W1-02 ✅: /listings/[id] owner detail (listings.getOwn; missing/foreign id → notFound = IDOR guard) with legal-action gating (features/listings/manage.ts, unit-tested, mirrors API state machine) — publish (listings.publish) when allowed + change-price with optimistic concurrency (listings.changePrice + expectedVersion; raced version → reload-conflict message), both Server-Action forms; listings table title links to detail. Boost FLAGGED as SDK gap (startBoost needs boost-tier lookup + wallet-captured paymentTxnId the seller SDK can't source; control omitted, noted unavailable, not faked). TC-W2-01 ✅: /orders/[id] seller order detail (orders.get; missing/foreign id → notFound = IDOR guard) — line items + server-computed totals (formatMoneyMinor), lifecycle transitions surfaced only when legal (features/orders/lifecycle.ts, unit-tested, mirrors order_status state machine): confirm/packed/ready/delivered/complete/cancel as idempotent Server Actions (Idempotency-Key; 409 illegal-move degrades to message); shipment section views status/AWB/delivered-at + deliver form (buyer OTP + optional PoD photo via MediaUploader single mode → shipments.deliver). AWB read-only (SDK has deliver but no AWB-setter — not faked); orders table order# links to detail. TC-W2-02 ✅: /offers seller incoming offers — listing picker (incoming offers are per-listing: offers.list incoming requires listingId, so picker → /offers?listingId= per-listing inbox; no faked cross-listing inbox), /offers/[id] detail (offers.get; missing/foreign → notFound = IDOR) with accept/counter(minor-unit, float-free)/reject Server Actions while negotiable (features/offers/negotiation.ts, unit-tested); accept → server creates order → links to /orders/[convertedOrderId]; 409 races degrade to message; no Idempotency-Key on counter/accept/reject (SDK exposes none — only offers.make). Offers added to sidebar nav. TC-W3-01 ✅: /payouts money-OUT — payout history (payouts.list, keyset) + tokenised destinations (bankAccounts.list) loaded in parallel (each degrades independently); request-payout Server Action (payouts.request, Idempotency-Key, float-free major→minor) + add-destination Server Action (bankAccounts.add, Idempotency-Key) by GATEWAY vaultRef + masked display fields only — NO raw account numbers/VPAs accepted (tokenised at gateway out-of-band); pure features/payouts/form.ts unit-tested. Payouts added to sidebar nav. TC-W3-02 ✅: /wallet settlements/payments ledger (payments.list, keyset) via DataTable, money formatMoneyMinor; running wallet balance FLAGGED as SDK gap (no wallet/balance/ledger-summary read in seller SDK — ledger shipped only, not a faked balance). Wallet added to sidebar nav. TC-W4-01 ✅: /auctions auctions-manage — list (auctions.list, keyset) + create (listing picker + form → auctions.create, Idempotency-Key, float-free money, datetime-local→ISO, end-after-start) + /auctions/[id] detail + bid history (listBids; sealed amounts masked) + approve/cancel surfaced only when legal (features/auctions/manage.ts, unit-tested, mirrors auction state machine; 409 degrades). env.featureAuctions flag gates sidebar entry + pages (API auctions flag authoritative — off → degrade-empty). Auctions added to sidebar nav (flag-gated). TC-W4-02 ✅: /disputes tenant disputes-moderation (DisputesResource.list box=all, keyset) + seller rating (auth.me→reviews.summary), parallel + independent degrade; /disputes/[id] detail (disputes.get; missing/foreign → notFound = IDOR) with review/escalate/resolve surfaced only when legal (features/disputes/manage.ts, unit-tested, mirrors dispute state machine; 409 degrades); resolve carries decision (refund_full/refund_partial/replacement/rejected) + optional float-free amount (refunds move money server-side, Law 11). SDK gap FLAGGED: no seller-respond method (moderation actions only, not faked). Needs dispute.resolve (server re-checked). Disputes added to sidebar nav. TC-W4-03 ✅: /notifications staff inbox (notifications.inbox, keyset, unread filter, per-item mark-read Server Action; server-rendered payload title/body/deep-link, same-origin deep-link guard) + /notifications/preferences event×channel matrix (full-replace setPreferences) + quiet hours (setQuietHours), no-JS Server-Action forms; mirrors storefront notifications. Notifications added to sidebar nav. Wave 4 (engagement & oversight) complete. TC-W5-01 ✅: /billing tenant subscription + plan catalogue — currentSubscription (+ limits/usage) + plans (public+active) + listSubscriptions history loaded in parallel (independent degrade); apply/change-plan Server Action (tenancy.apply, Idempotency-Key; paid plan moves money server-side, Law 11); usage/limits read-out merges limits+usage maps (features/billing/plan.ts, unit-tested; missing cap → unlimited). Money via formatMoneyMinor. Billing added to sidebar nav. TC-W5-02 ✅: /team staff roster + role assignments (rbac.assignments; ?pending=1 = approval queue) loaded via DataTable (user/role/kyc/active/approval columns; pending rows get an Approve Server-Action button) + All/Pending filter; approve-pending Server Action (rbac.approveAssignment → revalidate /team; 409 → illegal message) and admin-add-member Server Action (buildAddUser float-free phone normalise /^\\+?\\d{8,15}$/ in features/team/form.ts, unit-tested → users.create, Idempotency-Key) for staff who can't self-register. SDK gap FLAGGED: no role catalogue read + no direct assign/revoke method — only approve-pending + add-member offered (not a faked assign/revoke matrix). Team added to sidebar nav. TC-W5-03 ✅: /kyc own profile + verification — auth.me() shows display name/roles/locale; edit-profile Server Action (users.updateMe — token-resolved subject, no IDOR) posts a PII-minimal validated patch (name/email/dob/gender/language + optional avatar via the listings media flow in single mode → photoMediaId); kyc.list() renders the caller's docs + statuses with masked doc numbers (docNoMasked only, never raw) + reject reason; pure features/profile/form.ts (buildProfilePatch drops blanks/rejects no-op/validates email+dob+gender+language; kycStatusKey guards unknown statuses) unit-tested. SDK gap FLAGGED: kyc.submit needs a docTypeId but the SDK has no doc-type catalogue to enumerate choices (mobile flagged the same), so submission is noted unavailable, not faked. KYC added to sidebar nav. TC-W6-01 ✅ (DoD closed → web-tenant flips 🟡→✅): all 11 pure features/** modules unit-tested 1:1 (orders/lifecycle, listings/manage+form, offers/negotiation, auctions/manage, disputes/manage, billing/plan, payouts/form, team/form, profile/form, nav/safe-next); every fetching route segment has its own loading.tsx (20 pages ↔ 20 boundaries) over root loading/error/not-found; noindex enforced app-wide via root-layout default robots{index:false} + per-page generateMetadata; a11y (semantic landmarks, skip-link, labelled controls, keyboard-navigable forms, role=status/alert live regions). Final §4 sweep GREEN: i18n parity 468×en/hi/gu (no missing/extra/dupes), no token in any client bundle, no stray process.env, no inline styles, money only via formatMoneyMinor. Remaining tenant-config + vertical-operator surfaces (commission-rules/delivery-zones/branding/integrations/webhooks/billing-config/staff-permissions/languages; dairy-MCC/labour-employer-admin/schemes-assistant/ambassadors-admin/group-lots/auditor/ai-review-queue) stay SDK-gap-flagged — no seller-facing SDK method, out of scope, never faked. All copy via i18n, no inline styles. Money via formatMoneyMinor (string minor units), dates via formatDate. Degrade-never-die; noindex. Real next.config (transpilePackages + security headers). jest+ts-jest unit suite (safe-next open-redirect guard). §4 self-audit clean: i18n parity, no token in client bundle, server-only on server libs, no stray process.env, no inline styles. Next/React compiles under CI; TSX syntax-parsed clean offline, no broken local imports. Remaining waves TC-W1..W6 tracked in TENANT_BUILD_BACKLOG.md) |
| apps/web-admin | ✅ (Next.js 14 App Router god-mode console on the SEPARATE admin-api realm (Law 11) — ships its own minimal admin-client.ts, not the tenant SDK. Login links to FIDO2 IdP (hardware-key + step-up enforced server-side; no password in UI), server-gated dashboard, AI model registry (GET /v1/ai/models, keyset), model fairness report (GET /v1/ai/models/:id/fairness: stored audit + 30-day inference roll-up). admin-client: server-only bearer, timeout, idempotent-GET retry, typed AdminApiError (403→needsElevation) with no token leak. httpOnly Secure SameSite=Strict admin cookie. Other nav (tenants/flags/recon/audit) shown "(soon)", not links. noindex + robots Disallow. Real next.config (no-referrer headers). Hits real ai-models-ops endpoints; Next/React compiles under CI; TSX syntax-parsed clean offline. **AD-W0-01 foundation refit ✅**: central i18n catalog (src/i18n/en.ts + server Translator lib/i18n.ts, en-primary internal realm, NO hardcoded literals); red-brand elevation-aware Sidebar (features/nav nav-model, unit-tested) linking ONLY to built routes (others render non-link "(soon)"); root loading/error/not-found boundaries (error.tsx offers retry + re-authenticate for expired/insufficient session); admin-client extended with server-only mutation helpers adminPost/adminPatch/adminPut/adminDelete (mandatory audit `reason` in body, optional Idempotency-Key header, NO auto-retry — mirrors admin-api which validates reason server-side); login/dashboard/ai-models[/id] rewritten onto the shell (all copy via i18n, no inline styles, status pills/notice mapping via features/nav adminNoticeKey); jest + nav-model unit test. §4 self-audit clean (no literals/token-leak/stray-env/inline-styles; both libs server-only). **AD-W1-01 tenants-console ✅**: /tenants (GET /v1/tenants, keyset + status-chip filter, platform-wide read Law 11) + /tenants/[id] scorecard (GET /v1/tenants/:id — status/risk/subscription price via formatMoneyMinor/liveListings/openDisputes/limit-overrides; 404→notFound). Lifecycle approve/suspend/archive surfaced only when legal (features/tenants/tenant.ts mirrors admin-api tenant.state machine, unit-tested) as Server-Action forms with mandatory audit reason → adminPost :id/approve|suspend|archive; set quota-limit override → adminPatch :id/limits (buildLimitOverride: float-free integer-string, -1=unlimited, mirrors zod DTO). 403→needsElevation re-auth notice, 409→illegal-transition message; mutations carry no Idempotency-Key (admin-api exposes none here) + never auto-retry. /tenants flipped live in the nav model. **AD-W2-01 platform-reports ✅**: /reports cross-tenant exec dashboards — overview (GET /v1/reports/overview: MRR/ARR + active subs/tenants/users + login-success), GMV (gmv: GMV/platform-fee/commission/orders/AOV), tenant-growth (tenant-growth: new tenants/month) fetched in parallel, each degrading INDEPENDENTLY; money via formatMoneyMinor (minor-unit strings), login-success via float-free bpsToPercent (integer basis points). Regulator-export download served by a server Route Handler (/reports/regulator-export: GET /v1/reports/regulator-export → JSON attachment, content-disposition; bearer server-side only, 403→needsElevation JSON, never leaks token). Pure features/reports/report.ts (bpsToPercent + buildReportQuery) unit-tested. /reports flipped live in nav. **AD-W2-02 feature-flags ✅**: /flags registry (GET /v1/flags, prefix+enabled filter, keyset) + create (POST, defaults OFF/0% Law 10) + /flags/[key] detail + change history (GET :key + :key/history; 404→notFound). PATCH actions enable/disable/set_rollout/set_targeting/kill/unlock surfaced ONLY when legal (features/flags/flag.ts mirrors flag.entity kill-switch lock semantics, unit-tested) as Server-Action forms with mandatory audit reason → adminPatch flags/:key {action,...}; rollout is float-free integer 0..100, targeting allowlists charset+bound-validated. 403→needsElevation, 409→locked message; no Idempotency-Key (admin-api exposes none) + no auto-retry. /flags flipped live in nav. **AD-W3-01 recon-monitor ✅**: /recon money-safety — overview (GET /v1/recon/overview: latest run per type + ledger zero-sum health, balanced=false alarm) + /recon/runs[/[id]] (run list + detail/mismatch payload, open-investigation form POST /recon/investigations) + /recon/investigations[/[id]] (queue + work actions start/resolve/false_positive PATCH :id surfaced only when legal via features/recon/recon.ts mirroring investigation.state, unit-tested) + /recon/accounts/[id] (wallet drill-down + freeze/unfreeze control POST :id/freeze). Money via formatMoneyMinor (minor-unit strings); recon never posts the ledger. Mutations carry mandatory audit reason; 403→needsElevation, 409→illegal message; no Idempotency-Key (admin-api exposes none) + no auto-retry. One /recon/loading.tsx covers all nested segments. /recon flipped live in nav. **AD-W3-02 billing-ops ✅**: /billing SaaS billing — revenue overview (GET /v1/billing/revenue: MRR/ARR + outstanding/collected + invoice status counts) + /billing/invoices[/[id]] (list + detail/dunning history, issue/mark_overdue/void surfaced only when legal via features/billing/billing.ts mirroring invoice.state, unit-tested; record-dunning form while collectible) + /billing/adjustments (ledger + post-adjustment money move POST /billing/adjustments). buildAdjustment is money-safe (amount = minor-unit digit STRING, float-free) + Server Action attaches a fresh idempotencyKey in the body (no double-post); money via formatMoneyMinor. Mutations carry mandatory audit reason; 403→needsElevation, 409→illegal; PATCH invoice/dunning carry no Idempotency-Key (admin-api exposes none) + no auto-retry. One /billing/loading.tsx covers nested segments. /billing flipped live in nav. **AD-W4-01 plans-ops ✅**: /plans SaaS plan catalogue (GET /v1/plans + feature catalogue GET /v1/plans/features, status filter + keyset) + /plans/[id] detail + composition (features/limits) + change history. Create (POST), lifecycle publish/archive/reactivate (PATCH :id surfaced only when legal via features/plans/plan.ts mirroring plan.state, unit-tested), pricing (PATCH :id/pricing), new version (POST :id/version), feature set/clear (PUT/DELETE :id/features/:code), limit set/clear (PUT/DELETE :id/limits/:code). Money-safe: all price fields are minor-unit digit STRINGS (float-free, formatMoneyMinor display); limit value is whole-number string or -1 (rendered raw — no Number() precision loss). Mutations carry mandatory audit reason; 403→needsElevation, 409→conflict; no Idempotency-Key (admin-api exposes none) + no auto-retry. One /plans/loading.tsx covers nested. /plans flipped live in nav. **AD-W4-02 providers-ops ✅**: /providers integration-provider registry (GET /v1/providers, category + active filter, keyset) + two finance lenses /providers/health (GET /v1/providers/health) + /providers/financial (GET /v1/providers/financial) — every provider with credential-ref coverage counts (NEVER secret material); degraded = disabled-but-still-referenced (persisted config health, not real-time latency), surfaced first/alarmed. /providers/[code] detail + change history (GET :code + :code/history; 404→notFound). The one consequential write — enable/disable (PATCH :code, Law 12: pull a failing provider out of rotation platform-wide) — surfaced ONLY when it is a real change (features/providers/provider.ts mirrors admin-api no-op rejection; canEnable/canDisable + degraded health-key, unit-tested) as a Server-Action form carrying mandatory audit reason → adminPatch providers/:code {action,reason}. 403→needsElevation re-auth (admin-api requires providers.manage + FIDO2 + step-up), 409→already-in-state message; no Idempotency-Key (admin-api exposes none) + no auto-retry. One /providers/loading.tsx covers nested. /providers flipped live in nav. **AD-W4-03 support-oversight ✅**: /support cross-tenant NOC — ticket queue (GET /v1/support/tickets, tenant/status/severity/SLA-breach/assigned filters + keyset) + /support/sla-breaches (sla-breaches: still-working tickets past an unsatisfied SLA due date, most-urgent first) + /support/tenant-health (tenant-health: top tenants by open SLA breaches — open/breached/P0-open counts + oldest-open age via float-free ageParts). /support/tickets/[id] detail + server-computed SLA state (404→notFound). The one consequential write — escalate (POST tickets/:id/escalate: raise-only severity / status→escalated / reassign to platform lead) — surfaced ONLY when escalatable (features/support/ticket.ts mirrors ticket.state + sla raise-only rules; canEscalate/higherSeverities/buildEscalate, unit-tested) as a Server-Action form carrying mandatory audit reason → adminPost support/tickets/:id/escalate {severity?,reassignToUserId?,reason}. 403→needsElevation re-auth (admin-api requires support.oversight.manage + FIDO2 + step-up), 422→nothing-to-escalate/would-lower message, 409→illegal transition; no Idempotency-Key (admin-api exposes none) + no auto-retry. Support is a money-free helpdesk. One /support/loading.tsx covers nested. /support flipped live in nav. **AD-W5-01 compliance-ops ✅**: /compliance god-mode DPDP plane (PII-minimal — statuses/categories/UUIDs/counts only, never raw subject data). DSR queue (GET /v1/compliance/dsr, status+type filter, keyset) + /compliance/dsr/[id] (:id; 404→notFound) with start/complete/reject (PATCH dsr/:id) surfaced only when legal (features/compliance/compliance.ts mirrors dsr.state; erasure cooling-window 409). Export approvals /compliance/exports (GET exports) with inline approve/reject (POST exports/:id/decision, decidable only while pending). Breaches /compliance/breaches (GET breaches + open-incident POST breaches, data CATEGORIES only) + /compliance/breaches/[id] (:id; 404→notFound) contain→notify→close (PATCH :id; notify requires both DPDP §8 timestamps). Retention /compliance/retention (GET + upsert POST retention; whole-month windows, float-free unary-plus parse, no Number()/parseInt). Read-only audit explorer /compliance/audit (GET audit; actor/entity/action/tenant/time filters via GET form, partition-pruned keyset; PII-free payloads). DSR/breach/export/retention builders + state machines unit-tested (compliance.spec.ts, 30 asserts via node port). Mutations carry mandatory audit justification; 403→needsElevation (compliance.manage + FIDO2 + step-up), 409→conflict, 422→invalid; no Idempotency-Key + no auto-retry. One /compliance/loading.tsx covers nested. /compliance flipped live in nav. **AD-W5-02 impersonation ✅**: /impersonation god-mode act-as register (highest-sensitivity; prominent warning). Grant list (GET /v1/impersonation/grants, status filter, keyset) + mint form (POST grants — read-only scope only, time-boxed 60–3600s float-free, ≥8-char justification) + /impersonation/grants/[id] (:id; 404→notFound) showing the grant + its per-action audit trail (:id/actions). End (:id/end) + revoke (:id/revoke) surfaced only while active (features/impersonation/grant.ts mirrors grant.state — only active closes; canEnd/canRevoke/buildStartGrant/buildReason unit-tested, 17 asserts via node port). SECURITY: POST grants returns the scoped act-as token ONCE → the Server Action reads only grant.id and DELIBERATELY DISCARDS the token (never serialised to props/HTML/client/log; §4 token-leak sweep clean). 403 distinguishes kill-switch/privileged-target/elevation by AdminApiError.code, 409 active-grant-exists, 422 self/scope/ttl; no Idempotency-Key + no auto-retry. recordAction is machine-only and not surfaced. One /impersonation/loading.tsx covers nested. /impersonation flipped live in nav. **AD-W5-03 announcements ✅**: /announcements platform-wide notices — list (GET /v1/announcements, status+severity filter, keyset) + live-now strip (GET /active) + create form (POST, starts draft) + /announcements/[id] (:id; 404→notFound) detail + change history (:id/history). Edit (PATCH :id) offered only while draft/scheduled (published = immutable → 409); lifecycle schedule/publish/expire/archive (POST :id/*) surfaced only when legal (features/announcements/announcement.ts mirrors announcement.state; canEdit/canSchedule/canPublish/canExpire/canArchive + plain-text/audience/schedule builders, unit-tested — 28 asserts via node port). Content is PLAIN TEXT (angle-bracket HTML guard mirrored client-side), audience = plan/country allowlists, schedule windows ISO 8601 UTC. Mutations carry mandatory audit reason; 403→needsElevation (announcements.manage + FIDO2 + step-up), 409→illegal/immutable, 422→invalid; no Idempotency-Key + no auto-retry. One /announcements/loading.tsx covers nested. /announcements flipped live in nav. **AD-W6-01 global-catalogue-ops ✅**: /catalogue PLATFORM master taxonomy (ripples into every tenant). Two registries: lookup vocab — /catalogue (GET /v1/catalogue/lookup-types keyset + create) + /catalogue/lookup-types/[code] (rename PATCH :code + values GET lookup-values?typeCode + add-value POST) + /catalogue/lookup-values/[id] (edit PATCH :id + activate/deactivate POST :id/active + history :id/history; 404→notFound) — and the category tree — /catalogue/categories (GET categories, commerce-kind+active filter, keyset + create POST) + /catalogue/categories/[id] (children :id/children + edit PATCH :id + move POST :id/move cycle/depth-checked + activate/deactivate POST :id/active + history; 404→notFound). features/catalogue/catalogue.ts mirrors the DTO + domain: code/slug regexes, flat-JSON meta guard, float-free sortOrder/minAge (no Number()/parseInt), buildCreate/Update for type/value/category + setActive + move; unit-tested (catalogue.spec.ts, 24 asserts via node port). Mutations carry mandatory audit reason; 403→needsElevation (catalogue.manage + FIDO2 + step-up), 409→conflict (dup code/already-in-state/inactive-parent/active-children), 422→invalid (input/depth/cycle/subtree); no Idempotency-Key + no auto-retry. One /catalogue/loading.tsx covers nested. /catalogue flipped live in nav. **AD-W6-02 schemes-registry-ops ✅**: /schemes-registry PLATFORM government-scheme master. Authorities — /schemes-registry (GET authorities, level filter, keyset + create) + /schemes-registry/authorities/[id] (edit PATCH :id + history; 404→notFound). Schemes — /schemes-registry/schemes (GET schemes, active filter, keyset + create POST) + /schemes-registry/schemes/[id] (edit-meta PATCH :id no-version-bump, edit-rules POST :id/rules version-bump, set-window POST :id/window, activate/deactivate POST :id/active, history :id/history; 404→notFound) + read-only /schemes-registry/calendar (GET schemes/calendar open-on-MM-DD). features/schemes-registry/scheme.ts mirrors DTO+domain: code/MM-DD/season regexes, non-empty JSON-object guard, money-safe digit-string fee (no Number()/parseInt), buildWindow + create/update builders; unit-tested (scheme-registry.spec.ts, 28 asserts via node port). Fee rendered via formatMoneyMinor (Law 2). Mutations carry mandatory audit reason; 403→needsElevation (schemes.registry.manage + FIDO2 + step-up), 409→conflict (dup code/already-in-state), 422→invalid (input/non-active category); no Idempotency-Key + no auto-retry. One /schemes-registry/loading.tsx covers nested. /schemes-registry flipped live in nav. **AD-W6-03 cells-ops ✅**: /cells PLATFORM CELL MAP — the per-country routing stacks that form each data-residency (DPDP) boundary, the shards inside them, and which tenant lives where. Cells — /cells (GET /v1/cells/cells, country+status filter, keyset + create [residency-lock defaults ON]) + /cells/cells/[id] (edit PATCH :id [code/country immutable], status POST :id/status over the node state machine active↔readonly↔draining→retired, make/unset default POST :id/default, residency-lock toggle POST :id/residency-lock, history; 404→notFound). Shards — /cells/shards (GET shards, cell+status filter, keyset + create) + /cells/shards/[id] (edit PATCH :id [weight/notes] + status + history; 404→notFound). Placements — /cells/placements (GET placements, cell/shard filter + place POST placements) + /cells/placements/[tenantId] (move POST :tenantId/move + remove DELETE :tenantId; 404→notFound). Read-only /cells/residency (GET residency-report, per-country DPDP posture + at-risk flag). features/cells/cell.ts mirrors node.state + DTO: NODE_STATUSES + canTransition/statusTargets/acceptsPlacement, code/country regexes, float-free parseCapacity/parseShardIndex/parseWeight (no Number()/parseInt), build Create/Update/SetStatus/SetDefault/SetResidencyLock/CreateShard/UpdateShard/Place/Move/Remove + residency warn/at-risk helpers; unit-tested (cell.spec.ts, 40 asserts via node port). SECURITY (Law 11 + §4): a shard's dsn_secret_ref is a vault secret admin-api never emits — the console only shows a `hasDsn` boolean and offers NO field to set one. Unlocking residency / moving a tenant crosses a DPDP boundary → explicit warnings; admin-api enforces residency rules, shard↔cell match, capacity, node-accepting server-side. Mutations carry mandatory audit reason; 403→needsElevation (cells.ops.manage + FIDO2 + step-up), 409→conflict (dup code/index, node not accepting/empty, capacity, already placed), 422→invalid (illegal transition / residency violation / shard-cell mismatch); placement remove uses adminDelete with a body reason; no Idempotency-Key (admin-api exposes none) + no auto-retry. One /cells/loading.tsx covers nested. /cells flipped live in nav — the liveNav/soonNav partition now has NO remaining "(soon)" entries. **AD-W6-04 ai-models-actions ✅**: the existing /ai-models[/id] gains its two consequential mutations. /ai-models/[id] now surfaces PROMOTE (POST :id/promote — the <select> offers ONLY the model state machine's legal next states shadow→canary→production→retired via transitionTargets) and TUNE THRESHOLD (PATCH :id/threshold — a nullable 0..1 confidence ratio that gates human-review routing; blank clears it). features/ai-models/model.ts mirrors ai-model.state + the Promote/TuneThreshold zod DTOs: MODEL_STATUSES + canTransition/transitionTargets/isServing/isTerminal + statusKey/tone, decimal-threshold parse (regex + unary `+`, range 0..1, no Number()/parseFloat), float-free formatThreshold4 + formatPercent2 (integer Math.round + string padding, no toFixed — confidenceThreshold/overrideRate are RATIOS not money), buildPromote (legal-transition-gated) + buildTuneThreshold; unit-tested (model.spec.ts, 35 asserts via node port). The list + detail pages were refactored off `.toFixed`/the stale status map onto these shared helpers, so the whole /ai-models tree now passes the §4 float-coercion sweep. Both writes are Server-Action forms with a mandatory audit reason; 403→needsElevation (ai.model.manage + FIDO2 + step-up), 409→conflict (illegal transition/already in state), 422→invalid; no Idempotency-Key (admin-api exposes none) + no auto-retry. **AD-W7-01 admin-test-and-polish ✅ → web-admin DoD CLOSED**: console-wide quality sweep. Test coverage: all 16 pure features/** modules have unit specs (state machines, money/metric math, nav/elevation model — node-port verified). Boundaries: root loading.tsx/error.tsx/not-found.tsx cover every nested segment (error.tsx is a client boundary offering retry + re-authenticate, never logs PII/token), plus section-level loading.tsx. noindex: all 57 page.tsx carry metadata.robots {index:false} + root-layout metadata + a realm-wide X-Robots-Tag: noindex,nofollow,noarchive header (triple defence) + public/robots.txt. a11y: skip-to-content link, html lang, <main id=main> landmark, Sidebar <nav aria-label>, labelled controls, status chips carry text. needsElevation: 403→AdminApiError.needsElevation surfaced as a re-auth notice on every page + the error boundary's re-authenticate action. §4 sweep clean across the whole app: no secret/token in the browser bundle (admin-client + admin-auth both `import 'server-only'`; bearer attached only inside admin-client), NO stray process.env outside lib/env.ts (NODE_ENV gate centralised as env.isProduction), no inline styles, no float-coercion on money (the few non-money integer/ratio reads were moved onto digit-string rendering / unary-plus / integer-math formatters so the grep is fully clean). Every nav surface live; liveNav/soonNav has zero "(soon)" entries. Verification gate per build-sandbox note: static §4 self-audit + per-module node-port unit tests (green); tsc --noEmit + next lint + next build + jest run in CI's pnpm toolchain. web-admin is now a finished god-mode console.) |
| apps/web-partner | ✅ (Next.js 14 App Router lender/insurer partner portal on the shared SDK: phone-OTP login (httpOnly Secure SameSite=Lax tokens), pipeline KPI dashboard (review-queue roll-up), loan queue (fintech/loan-applications?box=review, keyset), application detail with lender-decision Server Actions hitting real endpoints (review/approve[amount+cooling-off]/reject/disburse[Idempotency-Key, Law 3]). Money bigint minor units — formatMoneyMinor display + BigInt ₹→paise on approve (no float). Partner scoping server-side (RLS+RBAC). lib/env (fail-closed), lib/partner-auth (requirePartner), lib/api-client. noindex + robots Disallow. Real next.config. Long-tail surfaces (claims/disbursals/policies/portfolio/SLA/settings) documented as not-yet-built; no TODO/placeholder pages shipped. Next/React compiles under CI; TSX syntax-parsed clean offline. **A wave-by-wave build plan now lives in apps/web-partner/PARTNER_BUILD_BACKLOG.md** (financial + logistics partner verticals on the shared SDK; insurance flagged BLOCKED — no apps/api module). **PR-W0-01 foundation-refit ✅**: brought web-partner to the storefront/tenant/admin bar — central i18n catalog (src/i18n/en.ts + server Translator lib/i18n.ts, en-primary, NO hardcoded literals); persona-aware Sidebar (features/nav nav-model derived from the access-token `perms` claim — Lending group for loan.manage, Logistics group for logistics.manage, both for `*`; links only to built routes, others "(soon)"; unit-tested); root loading/error/not-found boundaries (error.tsx offers retry + sign-in-again); pure features/lending/application.ts mirrors the fintech loan-application state machine + ₹→paise BigInt money (Law 2) with the loan-queue decision Server Actions extracted to app/loan-queue/actions.ts; login/dashboard/loan-queue[/id] rewritten onto the shell (inline styles + literals removed); getPartnerPermissions decodes the JWT perms claim for NAV PERSONA ONLY (API re-enforces RBAC); env.isProduction centralises the NODE_ENV gate; api-client + partner-auth both `server-only`. jest wired (nav-model + lending specs; 30 asserts via node port green). §4 self-audit clean (no inline styles/stray-env/float-on-money/token-leak; all copy i18n; every page noindex). **PR-W1-01 loan-queue-and-decisions ✅**: the lender pipeline finished on the shell — /loan-queue now has box chips (review [default] / all; box=mine is applicant-only, not offered) + status chips (the loan state machine) + keyset, preserved across pages by the pure buildListQuery/queueHref helpers (LENDER_BOXES, isLenderBox; unit-tested, 10 more asserts); /loan-queue/[id] surfaces the four decisions (review→approve[amountApprovedMinor via BigInt + coolingOffHours]→reject[note]→disburse[Idempotency-Key]) ONLY when legal for the current status via the pure state machine, with per-action ok flashes and 409→illegal / 403→forbidden / 404 mapped to localized notices. Money via formatMoneyMinor; §4 clean. **PR-W1-02 loan-products-and-profile ✅**: read-only lending catalogue + lender registry — /products[/id] (GET fintech/loan-products, active-only toggle + optional partnerId filter; amount range via formatMoneyMinor, interest APR rendered from integer basis points float-free via formatAprBps, tenure window via pure helper) + /profile[/id] the platform lender registry (GET fintech/partners, partner-kind + active-only filters; bank/nbfc/mfi/insurer/amc/gold_loan) with a partner→products link. Mirrors the real controller (filter-based lists, NOT keyset). Pure features/lending/product.ts (PARTNER_KINDS, formatAprBps, formatTenureMonths, parse helpers; unit-tested, 14 asserts). /products + /profile flipped live in the persona nav (lending group). §4 clean (no inline styles/float-on-money/stray-env; all copy i18n; 8/8 pages noindex). **PR-W1-03 portfolio-and-repayments ✅ → lending vertical complete**: /portfolio (GET fintech/loans, box=all — RLS scopes to the partner's funded book; loan status chips active/overdue/closed/written_off + keyset) + /portfolio/[id] (loan facts incl. repaid = principal − outstanding via BigInt, APR via float-free bps, dates; repayment schedule GET :id/repayments with per-row balance = due − paid and overdue flagging [unsettled + past-due via ISO date-string compare]). Read-only — borrower-side :id/repay is NOT a partner action. Pure features/lending/loan.ts (LOAN_STATUSES, repaidMinor/repaymentBalanceMinor BigInt money, isRepaymentSettled/isPastDue/isRepaymentOverdue, buildLoanListQuery/portfolioHref; unit-tested, 25 asserts). /portfolio flipped live — the lending persona group is now fully built (soonNav empty for lenders). §4 clean (no inline styles/float-on-money/stray-env; all copy i18n; 10/10 pages noindex). **PR-W2-01 fleet-setup ✅ → logistics vertical begun**: /fleet (+/fleet/carriers/[id]) the 3PL carrier registry (logistics/partners GET/:id + create/PATCH + :id/active; kinds 3pl/tenant_fleet/rider) + /fleet/vehicles[/id] (logistics/vehicles; capacityKg is a WEIGHT — positive whole kg parsed float-free, NOT money; refrigerated flag) + /fleet/slots[/id] (logistics/pickup-slots; weekday + HH:MM window, start<end enforced by the pure builder via lexicographic compare on zero-padded times). All 9 mutations via Server Actions; POST creates carry an Idempotency-Key (Law 3). Pure features/logistics/fleet.ts (PARTNER_KINDS, normalizeRegNo, parseCapacityKg digit-regex float-free, validTime/buildCreateSlot, buildCreate/Update*; unit-tested, 35 asserts). /fleet flipped live in the persona nav (logistics group, gated by logistics.manage); shipments/zones remain (soon). §4 clean (no inline styles/float-on-money/stray-env/token-leak; all copy i18n; 16/16 pages noindex). **PR-W2-02 zones-and-routes ✅**: /zones[/id] (logistics/zones CRUD + :id/active, keyset + ?pincode filter; pincodes [6-digit] + region UUIDs parsed from space/comma blobs by the pure builder, optional charge-definition UUID) + /routes[/id] (logistics/routes CRUD + :id/active, keyset + run-weekday filter; nullable run-day, village region UUIDs, vehicle + consolidation-agent refs) + /cold-chain (the reefer/vaccine temperature log — SUBJECT-SCOPED list GET logistics/cold-chain/readings [subjectType+subjectId, breach-only toggle] + record form POST readings, append-only so NO Idempotency-Key; temperatures are physical decimals [°C] parsed float-free + rendered as plain strings, NOT money; API recomputes is_breach). POST creates on zones/routes carry an Idempotency-Key (Law 3). Pure features/logistics/network.ts (zone/route builders, parsePincodes/parseRegionIds UUID-validated, parseTempC decimal float-free, toIsoTimestamp, buildColdChainQuery subject scoping; unit-tested, 51 asserts). /zones + /routes + /cold-chain flipped live in the persona nav (logistics group); only /shipments remains (soon). §4 clean (no inline styles/float-coercion/stray-env/token-leak; all copy i18n; 21/21 pages noindex). **PR-W2-03 shipments-ops ✅ → logistics vertical complete**: /shipments (GET shipments assigned queue, box=all [tenant ops] / mine [rider] chips + the 11-status filter + keyset) + /shipments/[id] (facts incl. charge/COD via formatMoneyMinor from bigint-minor strings, cold-chain flag, timestamps) surfacing the delivery-lifecycle Server Actions (assign → schedule-pickup → picked-up → in-transit → at-hub → out-for-delivery → deliver / fail / cancel) ONLY when the pure state machine says the transition is legal; the API re-enforces it (409 → illegal-transition notice). Delivery is OTP-gated (deliver: buyer's 4–8 digit OTP + optional POD media UUID); assign needs ≥1 of carrier/vehicle/rider; schedule-pickup takes a datetime + window. These advance a state machine, not money, so NO Idempotency-Key (only shipment create exposes one, which this portal never does); mutations never auto-retry. Pure features/logistics/shipment.ts mirrors shipment.state's SHIPMENT_STATUSES + TRANSITIONS exactly (canTransition/isTerminal/availableActions + buildAssign/buildSchedulePickup/buildDeliver/buildFail; unit-tested, 45 asserts). /shipments flipped live — the logistics persona group is now fully built (fleet soonNav empty). §4 clean (no inline styles/float-coercion/stray-env/token-leak; all copy i18n; 23/23 pages noindex). **PR-W3-01 partner-test-and-polish ✅ → web-partner COMPLETE**: added the silent-refresh session gate (lib/session.ts — resolveSessionToken refreshes an expired access cookie server-side from the refresh cookie via anonClient().auth.refresh, re-writing the httpOnly cookies; requirePartner now async, redirecting to /login only when both cookies are dead), kept apart from lib/partner-auth (cookie helpers) to avoid an api-client import cycle; all ~28 page/Server-Action call sites await it. DoD sweep green: all 7 pure features/** modules unit-tested (application/loan/product + fleet/network/shipment state machines + nav persona model — ≈220 node-port assertions), root loading/error/not-found boundaries, 23/23 pages noindex, the 403-forbidden notice + 401-silent-refresh paths both verified, §4 clean (no inline styles/float-coercion/stray-env; api-client/partner-auth/session all server-only, none imported into a client component; all copy i18n). BOTH partner verticals fully built — lending (loan-queue+decisions / products / lender registry / portfolio+repayments) and logistics (fleet / zones / routes / cold-chain / shipment delivery lifecycle). Insurance remains out of scope (no apps/api module). Wave history PR-W0…W3 in PARTNER_BUILD_BACKLOG.md) |
| apps/mobile (React Native) | ✅ (Expo SDK 51 + expo-router, offline-first, vernacular hi/en/gu. FOUNDATION + auth + farmer-listings vertical built against the shared SDK. Core: config (fail-closed), api/client (live in-memory bearer + tenant header), offline-queue (durable, idempotency-keyed, retry+dead-letter — unit-tested), pure session.reducer (unit-tested), secure-store token store (Keychain/Keystore), auth.store provider (boot-restore + proactive refresh, fail-closed), i18n runtime + numerals (unit-tested), otp.helpers (E.164 + cooldown, unit-tested), role-switcher (unit-tested). Onboarding screens 01–05 (welcome/language/OTP/role/profile, enumeration-safe). Farmer tabs + home (09) + my-listings (12) + create-listing (10, catalogue picker, ₹→paise BigInt, OFFLINE-FIRST create) + listing detail (112) + orders/wallet/profile. Money = bigint minor via MoneyText (Law 2); degrade-never-die everywhere; idempotent mutations (Law 3). ui-native component library built (theme+10 primitives). 34 unit tests green offline; all TSX syntax-parsed clean, zero broken local imports; RN device build is CI-only (expo toolchain). Guide-compliance pass: feature-flag/kill-switch core (remote-hydrated, default-OFF for unbuilt verticals) gating the farmer tabs; ALL screen API calls moved into features/*.api data layers (no direct apiClient in screens); all user-facing strings i18n hi/en/gu (no literals); eslint-config-expo added; per-vertical "Threats considered" note in README. Remaining ~180 screens mapped in apps/mobile/SCREENS_BACKLOG.md + priority order in MOBILE_BUILD_ROADMAP.md; no fake screens wired. **Roadmap P-01 DONE**: core/media capture+upload (expo picker JIT-perms → compress/resize+EXIF-drop → pure SHA-256 integrity → presigned S3 PUT with progress+retry → confirm) + offline-first via shared dispatched queue core/offline/sync-queue (idempotent replay; listings migrated onto it) + ui-native UploadTile/AddMediaTile/ProgressBar. **Roadmap P-02 DONE**: offline read-cache (pure SWR cache-policies + Cache engine + expo-sqlite store, per-user scoped + cleared on sign-out = anti-IDOR) + core/connectivity (NetInfo) + sync.engine (replay queue on offline→online edge + foreground, concurrent-guarded) + ui-native OfflineBanner; farmer reads cached (wallet left live for money correctness). **Roadmap P-03 DONE**: SDK payments/payouts/kyc/bankAccounts resources; core/payments (Razorpay checkout boundary, capture verified server-side via webhook — client only polls) + pure money/status helpers; core/security FLAG_SECURE; add-money (20) + KYC-status (109/175) screens, flag-gated (payments_addmoney/kyc OFF). Flagged-not-faked gaps: wallet-balance read-model + KYC doc-type lookup + bank vaultRef tokenisation are unbuilt API endpoints (bank-add/withdraw/Aadhaar-eKYC deferred to P-06/Phase-2). 77 mobile unit tests green. **Roadmap P-04 DONE**: SDK notifications resource; core/push (Expo push register + pure notification-router with off-app deep-link guard + pure quiet-hours + foreground/tap listeners); ui-native Toggle; in-app notification center screens (inbox 28/191, detail 172, settings 171) + home bell, flag `notifications` OFF. Flagged-not-faked: no client device-token registration endpoint yet (syncPushToken degrades). **ENTIRE mobile roadmap since complete — P-05…P-32 DONE** (see apps/mobile/MOBILE_BUILD_ROADMAP.md): listings voice/photo create, wallet, orders, buyer browse/cart/checkout/pay, offers+chat, auctions, worker/labour, ambassador, education, owner tenant-lite, market/mandi+weather, tips/AI assistant, govt schemes, farmer profile/farm/bank/docs/help, global search + settings + DPDP system screens, **P-30** security hardening (TLS pinning/integrity/deeplink-clipboard guards + FLAG_SECURE on 17 sensitive screens + Hermes/R8/cleartext-off), **P-31** observability (redactPII + correlation + consent analytics + crash + SLO), **P-32** release pipeline (forced-update gate + OTA flag discipline + EAS phased rollout + Maestro sell+buy+pay e2e + store-compliance/data-safety). 305 mobile unit tests green; hi/en/gu i18n parity (1232 keys); all feature data behind default-OFF flags; no direct apiClient in screens. RN device/store build is CI/EAS-only. **M-W0 mobile-test-and-polish ✅ → flips 🟡→✅**: the DoD-close audit the web apps each ran. 305/305 pure unit tests GREEN (node-port offline run of every features/** + core/** spec); §4 sweep GREEN — 0 `apiClient()` in screens, no token logged, the only `Number()` in a money path is the documented Razorpay gateway-boundary paise cast (core/payments/checkout.ts), i18n parity 1232×3 zero drift, 0 hardcoded hex in screens (tokens-only), every vertical flag default-OFF + kill-switch; coverage GREEN — 165 routes across the 7 role groups, 17 FLAG_SECURE screens, security cores (pinning/integrity/deeplink-guard/clipboard-policy/screen-guard) present; SCREENS_BACKLOG ⬜/🟡 markers each reconciled to a backend-gap or Phase-2 row (nothing silently unbuilt). Phase-1 GA scope COMPLETE. Post-GA fast-follows (built-but-flagged, awaiting a backend endpoint: wallet read-model, KYC doc-type/bank vaultRef, push token-register, boost/analytics/LLM-voice, saves/seller-profile, attendance/clock-in, mobile analytics/broadcast, DPDP export/delete, …) + Phase-2 role apps tracked in apps/mobile/MOBILE_BUILD_BACKLOG.md. tsc/jest/eas build/Maestro run in CI/EAS) |
| packages/ui-native | ✅ (…theme + Button/Card/Input/OtpInput/MoneyText/StatusPill/EmptyState/SkeletonCard/ScreenScaffold/VoiceButton + P-01 media primitives: ProgressBar, UploadTile, AddMediaTile — tokens-only, a11y, ≥48px) |
| packages/ui-native | ✅ (RN component library on the Phase-1 design tokens: theme (full palette/space/type/shadow) + Button, Card, Input, OtpInput, MoneyText (bigint-minor, Law 2), StatusPill, EmptyState, SkeletonCard, ScreenScaffold, VoiceButton — ≥48px targets, a11y props, tokens-only. Compiles under CI; consumed by apps/mobile) |
| apps/realtime-gateway | ✅ (WebSocket fan-out, Phase 2 — END-TO-END, nothing faked. PUBLISHER (apps/api `core/realtime`, runs in the relay/worker): `RealtimeFanoutRegistrar` registers one `RealtimeFanoutHandler` per mapped eventType (auction bid/extend/end/won, order status x10, dairy collection/bill_paid) into the OutboxHandlerRegistry; on relay it projects the event to a NON-PII message (`projectEvent`: bidder/winner ids dropped, sealed-bid amount hidden, money STRING minor units Law 2) and publishes to a tenant-scoped Redis channel (`t:{tenant}:auction|u:{user}:orders|mcc:{id}`) — best-effort, resilience-wrapped, never throws into the outbox tx (Law 12), gated by `realtime_fanout` flag (default OFF, seeded) + REDIS_URL (else Noop). GATEWAY (this app): stateless ws pods — handshake auth verifies the SAME access JWT (HS256/iss/aud pinned, `typ:access`, fail-closed 4401); every subscribe goes through `canSubscribe` (TENANT ISOLATION first — cross-tenant deny; per-user orders owner-only no-IDOR; MCC requires `dairy.manage`/`*`); bounded subscriptions + 4KB frame cap + slow-consumer eviction (pure `backpressure/policy`); RedisPubSubAdapter PSUBSCRIBE `t:*` → per-pod fan-out; capped Redis-Streams replay for reconnects; /healthz + /metrics (no PII labels). 26 gateway unit tests (authz cross-tenant/owner/perm denials, JWT verify forge/expire/realm, backpressure) + 9 api projection/channel tests green; api typecheck clean. Cross-pod presence totals + authenticated replay-since API deferred. Live socket soak/load test is cluster-only) |
| apps/stream-processor | ✅ (Phase 2, END-TO-END Kafka stream tier — moves heavy fan-out off the write path. INGESTION: OutboxTailer keyset-reads outbox_events from a per-shard `stream_ingest_checkpoint` (append-only — never mutates status, so NO contention with the in-process dispatcher; multiple readers each track their own offset) and publishes each row to its ingest topic keyed by tenant_id (per-tenant ordering). RUNTIME: idempotent KafkaJS consumer-runtime (at-least-once → every (consumer,event_id) recorded in `stream_processed_events`, redelivery is a no-op; side effects idempotent), bounded exp-backoff retry → DLQ (`stream_dead_letters` + kv.dlq topic) so a poison message never blocks a partition (no head-of-line blocking). 5 CONSUMERS (own group each): search_indexer→OpenSearch upsert/delete-by-id; notification_fanout→external notifier (HTTP, dedups on idempotency-key); projection_builder→`stream_read_projections` CQRS read-models (monotonic source_event_id upsert, tenant-scoped via withTenantTx); fraud_signal→pure scoring (high-value/velocity/new-account/devices, bigint minor units Law 2) → produces kv.fraud.signals (advisory only, human review, NEVER auto-money-action, Law 11); analytics_etl→PII-stripped flatten → produces kv.analytics.events. Connects as kv_relay BYPASSRLS (cross-tenant infra) + RLS defense-in-depth + tenant_id on every row. Migration 0031 (checkpoint + dedup + DLQ + read-projections, tenant-scoped + RLS pass). Fail-closed config (KAFKA_BROKERS+DATABASE_URL). 21 pure unit tests green (topics/envelope/retry-policy/fraud-scoring); SQL balanced. Downstream landing (ClickHouse via analytics-pipeline; fraud queue via ai-governance; rich velocity features via ai-services feature-store) owned by consumers — honestly flagged, not faked. Live broker/DB soak is cluster-only) |
| apps/ai-services | ✅ (Phase 2, internal FastAPI inference tier — voice→listing extraction, photo grading, price bands, fraud signals — called service-to-service by api/worker/stream-processor; ADVISORY only, enforcement stays server-side with a human in the loop (Law 11). common/: fail-closed config (refuses to boot in prod without a strong API_SHARED_SECRET + inference-log DB), constant-time s2s bearer auth (401 fail-closed; trusted server-resolved tenant_id), redact (input_ref reduced to ALLOW-LISTED pointer keys + deep PII scrubber — raw transcript/audio/image NEVER persisted or logged), resilience (timeout+retry+circuit-breaker → degrade to needs_review), telemetry (JSON logs no-PII + metrics), asyncpg db (sets app.tenant_id per write so RLS holds; no cross-tenant reads), model_registry (resolve active ai_models row + confidence_threshold; unregistered ⇒ always review), inference_logger (writes ai_inferences pointers-only). price_bands: float-free nearest-rank P10/P50/P90 in INT minor units → strings (Law 2), mirrors market-intel baseline-v1. voice_extraction: STT→LLM via httpx (resilience-wrapped, degrade), output normalised/validated (never trust the model; price stays string minor units), confidence blend. photo_grading: image-metadata validate (fail-closed) + scores→grade argmax. fraud_signals: pure explainable scoring (bigint minor units) → advisory flag to the review queue, never auto-blocks. Each model below confidence_threshold → ai-governance human review. 31 pure unit tests green (price-band float-free math + band invariant, redaction/PII-scrub, constant-time auth + fail-closed config, voice parse/validate, photo grade, fraud rules, circuit-breaker); py_compile clean on all files. Boundaries flagged-not-faked: LLM/STT/CV providers run only when keyed; model TRAINING (training/, feature-store) + rich velocity/graph features are the feature-store's batch job; model lifecycle WRITES are admin-api (Law 11). Live serving needs declared deps + Postgres w/ migration 0013; offline suite runs under plain python3) |

\* `tenancy` (plans, subscriptions, plan_limits, billing) is technically Wave 1 because `quota` reads it — build it alongside/just after `identity`.
