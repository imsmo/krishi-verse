// modules/listings/controllers/v1/listings.controller.ts
// Versioned alias — canonical implementation lives one level up. Kept so the
// /v1 path in the manifest resolves to a single source of truth (no duplication).
export { ListingsController } from '../listings.controller';
