# apps/web-partner/.env.example · partner portal (no secrets in the client; only NEXT_PUBLIC_* is bundled)
NEXT_PUBLIC_API_URL=http://localhost:3000
# API_URL_INTERNAL=http://api.internal:3000  # optional in-cluster SSR origin (never shipped to the browser)
