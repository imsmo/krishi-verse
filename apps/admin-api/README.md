# README

GOD MODE backend — platform-owner plane. Separate app on purpose: own pods, own WAF rules, IP-allowlisted, hardware-key 2FA, NO tenant traffic shares this process. Connects as kv_admin (RLS-bypass capable, every query audited). · [P1]
