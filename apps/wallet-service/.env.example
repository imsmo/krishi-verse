# WALLET SERVICE — the only process with ledger write credentials.
# Note: different DB user (kv_wallet) than the api (kv_app). On purpose.
NODE_ENV=development
GRPC_PORT=50051
DATABASE_URL=postgres://kv_wallet:dev@localhost:5432/krishiverse
RAZORPAY_KEY_ID=rzp_test_xxx
RAZORPAY_KEY_SECRET=secret_test_xxx
RAZORPAYX_ACCOUNT_NO=test
RECON_HOURLY_CRON=0 * * * *
HOT_ACCOUNT_STRIPES=16          # platform escrow/fees striped across N rows
