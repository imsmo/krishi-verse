# infra/k8s/external-secrets/admin-api.yaml · sync AWS Secrets Manager -> k8s Secret krishiverse-admin-api-env (consumed by the admin-api chart)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: admin-api-env
  namespace: krishiverse
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: krishiverse-admin-api-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: krishiverse-prod/admin-api/env   # a JSON secret in Secrets Manager holding every env var for this service
