# capacity-plan

capacity model: pods/replicas/shards/cells per traffic tier, mapped to the A9 cost ladder; autoscaling targets; pre-provision schedule for known spikes (PMFBY window, harvest, festivals) · [P1]
