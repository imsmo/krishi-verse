{
  "name": "@krishi-verse/ui",
  "version": "0.1.0",
  "private": true,
  "description": "React component library (web) built on the design tokens.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "clean": "rm -rf dist"
  },
  "peerDependencies": { "react": "^18.3.1" },
  "dependencies": { "@krishi-verse/tokens": "workspace:*" },
  "devDependencies": { "typescript": "^5.5.4", "@types/react": "^18.3.5" }
}
